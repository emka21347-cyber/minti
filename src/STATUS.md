# MINTI — Project Status

> **Last updated:** 2026-06-11 (**Dist D0–D3 ALL SHIPPED — MINTI's two doors are ON THE INTERNET**: site live at **https://minti-pi.vercel.app**, downloads served from the public **github.com/emka21347-cyber/minti-releases** v0.4.0-D1 release (Windows zip + Linux tarball + dual-arch macOS tarballs + checksums.txt — all links verified 200). D1 Windows installer live-verified on the daily driver; D2 Linux/macOS artifacts built+verified (VM cycle + live-Mac run honestly deferred); D3 site = T3-generated + T2-reviewed (4 HTML bugs caught), zero JS/CDN/telemetry, 19 KB. **Remaining in milestone D: D4 only** (Calamares + firmware on the ISO — gated on the build VM). **Next session (user, 2026-06-11): site LAYOUT polish first → then D4; macOS live-verify deliberately LAST on a real Mac — handover + paste-verbatim kickstart in docs/plans/distribution-roadmap.md "Sequencing after D3".** Earlier today: Clan Memory M0–M6 ALL SHIPPED.)
> **Purpose:** Read this *first* when opening a new chat or onboarding to the project. It's the single document that tells you where MINTI is right now and how to pick up work without re-reading history.

---

## TL;DR

MINTI is a minimal, AI-agent-first Linux software stack plus a cross-OS **Clan protocol** for distributed local AI compute. **M0–M3 are done and validated** in the `minti-dev` Linux Mint VM. Install path works; `minti-runtime` serves OpenAI + Ollama + **Anthropic** (`/v1/messages`, M3) shapes; 5 stdio MCP tool servers route through a policy-gated framework; `minti-pack-recon` installs the nmap/whois/dig toolchain; **opencode** (sst, MIT) is bundled with a system-wide config that registers minti-runtime as a custom provider and all 5 MCP servers as stdio commands; **Claude Code preset** is documented in `docs/claude-code-preset.md` for users who already have it. **M4 core consensus validated** (2026-05-28): cland Phases 0 + A + B + C + D + E + F + G + H-1/2/3 + I + J + security hardening all shipped. **3-node Clan demonstrated** (2 Linux VMs + Windows 11 host); cross-OS election + failover working. **M5 (Cross-platform Clan Agent) shipped**: M5-A foundations + M5-B Windows NSSM-managed service (live-installed on the daily-driver host) + M5-C macOS launchd daemon (build clean, live Mac install deferred). **M6-content + M6.1 + M7 landed** (2026-06-06): DLC-style addon packs `minti-pack-{hermes3,mistral,wiki-simple}` + shared `minti-pack-fetch` helper (now its own .deb with restored `Depends:` chain) + new `minti-mcp-wiki` server + default-model fallback in runtime-adapter. **M7: minti-status TUI dashboard** — live read-only terminal dashboard surfacing Clan + Runtime + Ollama VRAM + Addons + Harness in one screen; bubbletea + lipgloss; all 3 platforms (linux/windows/darwin) cross-compile clean from day one; .deb at /usr/bin/minti-status. End-to-end dep-chain smoke (purge → dpkg-i-addon-fails → install pack-fetch.deb → install addons → markers + Addons line → purge) verified in the VM. minti-status verified against the live Phase J Clan in the VM — Clan/Orchestrator/term/lease/election-history all populate correctly. Kiwix ZIM pinned to wikipedia_en_simple_all_nopic_2026-05.zim. **M8 Phase 1 shipped + smoke-validated** (2026-06-06): knock-by-clan-id join flow — X25519 ECDH + HKDF-SHA256 + AES-256-GCM + 30-bit SAS + mutual confirmation. 14 unit tests. Smoke script `scripts/m8-phase1-smoke.ps1` passes 7/7: founder creates Clan, daemon starts (receiver), joiner knocks (no shared secret), derives SAS, confirms y, operator lists+accepts, receiver seals+delivers, joiner persists Clan state. CLI: `knock`, `knocks`, `knock-accept`, `knock-deny`. **M8 Phase 2 shipped + cross-node validated** (2026-06-06): `scripts/m8-phase2-validate.ps1` passes on real 3-node testbed (VM-A 192.168.56.101, VM-B 192.168.56.102, Windows host). Test 1: ephemeral Clan + daemon on VM-A. Test 2 PASS: VM-B knocks into VM-A (Linux→Linux), SAS matches, accept delivers sealed welcome, VM-B persists Clan state, VM-A roster updated. Test 3 PASS: Windows binary knocks into VM-A (Windows→Linux), knock arrives and is visible on VM-A (delivery skipped without admin firewall rule; re-run as admin for full delivery test). Tests 4+5: mismatch rejection + rate-limit spot check (informational). Root causes fixed: SSH nohup stdout/stderr redirect, pkill/pgrep self-match ([d] trick), Windows host-only IP detection for deliver listener, verbose poll diagnostics. **M9 shipped** (2026-06-06): distro image pipeline + visual identity — `docs/brand.md` formal palette, `assets/logo.svg` (9-node grid + wordmark), `assets/wallpaper-3840x2160.svg` (4K mesh), `assets/plymouth/minti/` script-plugin splash, `assets/grub-theme/minti/theme.txt` GRUB2 theme. `lbconfig/` source-controlled live-build config tree (auto/config, package-lists, chroot hook installing Ollama + MINTI + Plymouth/GRUB wiring, preseed, grub.cfg). `scripts/build-iso.sh` one-command build on VM-A (rasterizes PNGs, generates .pf2 font, runs lb build). `scripts/build-iso.ps1` Windows wrapper (rsync → VM-A → build → scp ISO back). **Phase D (live ISO boot test) PoC PROVEN** (2026-06-07, build #27 on VM-A=minti-dev-2/.101): ~360 MB lean ISO boots syslinux → kernel 6.1.0-47 + live-boot squashfs → MINTI `/etc/issue` banner → **autologin as `user`** → working shell; `minti-fetch` renders the cyan 9-node logo + sysinfo, `minti-cland show` returns a Member ID, 6 MCP servers present. Verified via serial console + VBox GUI. Root cause of the long login-failure hunt: build `umask 007` left `/usr` at `0770`, blocking all non-root exec — fixed via `umask 022` + chroot perms normalization. Other fixes folded into `lbconfig`/`build-iso.sh`: `--initsystem systemd`, isolinux template override, `boot=live`+`plymouth.enable=0`+serial console, binary-stage tools in chroot pkg list (syslinux/genisoimage/xorriso/mtools/etc), mask live-config (it broke nss + sshd), `ssh-keygen -A`, mawk. **Pending**: SSH remote pubkey auth (console works, remote refused — StrictModes-class quirk), remove `minti-poc-demo.service` debug scaffolding, and **commit the lbconfig/build-iso.sh changes** (currently working-tree only). Full detail in `memory/reference_live_build.md`. **Honestly not yet proven**: M9 Phase D live ISO boot (in progress this session); full 16-test acceptance gate (Phase J Pass 4, **M5-D Phase 4 rerun paused, pending VM bring-up**; validate-script JSON fixes are on disk), >24h Defender behavioural-block soak, cold-boot-no-login mDNS, real Ollama pull through `minti-pack-fetch hermes3`, real Kiwix ZIM download end-to-end with strict SHA-256 verify, live macOS install, minti-status under tmux/screen + over SSH non-TTY, minti-status interactive (vs --once) for long enough to exercise the tickers. After M9: **M5-D resume** (VM bring-up + m5-phaseD-validate.ps1), then M4.1 (Cardputer demo) or M3.5 (tool-use content blocks) or M6-broader (signed repos) or M7.7 (.gif / vm_stat).

---

## Source of truth

The PRD is the authoritative spec. **Read it before any implementation work:**

- **Path:** `C:\Users\aouad\.claude\plans\hello-can-we-create-abundant-hopper.md`
- **Version:** **v0.6** (current)
- **Core principle (P1):** Open-source-first. MINTI never *depends* on a closed-source service. Default agent clients (`opencode`, OpenHands), default models (Hermes 3, Llama, Qwen, DeepSeek), default runtime (Ollama / llama.cpp), and default transport are all open. Proprietary services (Claude Code, Anthropic API, OpenAI) are optional integrations users can choose to bring — never bundled, never required.
- **Other principles:** P2 (resurrection of dead hardware — installable on 1–2 GB old machines), P3 (PRD wins over clever-code).

---

## Current milestone

### Done ✅
- **M0** — Repo skeleton, Clan Protocol Spec v0.1 (`docs/clan-protocol.md`), `install.sh` v0
- **M1** — `runtime-adapter/` Go daemon. Backend interface + Ollama implementation + OpenAI-compatible + Ollama-compatible HTTP API, with systemd unit, config schema, install integration. Builds clean on Windows + cross-compiles to Linux amd64. All endpoints smoke-tested end-to-end.
- **Validation pass** — Full install + chat smoke test on real Debian 13 in WSL2. Two install bugs caught + fixed.
- **Dev environment** — Linux Mint 22.3 Xfce VM in VirtualBox with the entire MINTI stack installed.

- **M2 — MCP servers + first tool pack** (2026-05-26 ✓ validated end-to-end in the `minti-dev` VM)
  - 5 MCP servers under `mcp-servers/cmd/mcp-{fs,shell,recon,http,pkg}/`, each speaking stdio MCP via the official `github.com/modelcontextprotocol/go-sdk` v1.6.1.
  - Shared framework in `mcp-servers/internal/{policy,audit,permission,mcpserve,proc}/`. Every tool call routes through policy → audit transparently.
  - `mcptest` stdio test harness in `cmd/mcptest/` — renders consent prompt on TTY.
  - `minti-pack-recon` debian metapackage in `packs/recon/debian/` — Depends: nmap, masscan, whois, dnsutils|bind9-dnsutils. Recommends: theharvester, amass. Suggests: rustscan, golang-go.
  - `install.sh` extended: stages MCP binaries to `/opt/minti/mcp/`, installs `mcptest` to `/usr/local/bin/`, writes default `/etc/minti/policy.yaml`, prepares `~/.minti/` for the invoking user.
  - `docs/clan-protocol.md` §7.2 extended with `deny_tools` per-namespace kill switch + `allow_raw_socket` recon gate + system+user policy overlay.
  - **Acceptance:** `mcptest --yes --arg target=127.0.0.1 /opt/minti/mcp/minti-mcp-recon nmap_scan` ran a real `nmap -sV -T3 --top-ports 1000 127.0.0.1` (returned 22/ssh + 631/cups in 6.2s) with the call audited.
  - **Deny path:** same call with `mcp.recon.deny_tools: [nmap_scan]` in `~/.minti/policy.yaml` → refused before nmap spawn; audit log shows `decision: deny, reason: tool 'nmap_scan' is on recon.deny_tools`.

- **M3 — Agent client integration** (2026-05-26 ✓ validated end-to-end in the `minti-dev` VM)
  - **Anthropic `/v1/messages` surface added to `runtime-adapter`** (non-streaming + SSE). Test bench: real `llama3.2:3b` returned a clean Anthropic-shaped envelope with `type: message`, proper `stop_reason`, usage stats; SSE emitted all 6 event types in spec order (message_start, content_block_start, 12 content_block_delta, content_block_stop, message_delta, message_stop). Tool-use content blocks in messages are rejected with a clear 400 — deferred to M3.5.
  - **opencode v1.15.10 (sst, MIT) bundled** via the official installer, symlinked from `~/.opencode/bin/opencode` to `/usr/local/bin/opencode` so all users find it on PATH. System config template at `/etc/minti/opencode.config.example.json` + per-user default dropped at `~/.config/opencode/opencode.json` (preserved if already present). Config registers `minti-runtime` as a custom `@ai-sdk/openai-compatible` provider at `http://127.0.0.1:7780/v1` and all 5 stdio MCP servers by path.
  - **Claude Code preset** documented at `docs/claude-code-preset.md` — env-var (`ANTHROPIC_BASE_URL=http://127.0.0.1:7780`) + `claude mcp add` recipe. Tool execution flows directly via stdio to `/opt/minti/mcp/*`; chat reasoning flows through `/v1/messages` against the local model.
  - **install.sh idempotency hardening**: now compares the new on-disk binary's hash to the running process's `/proc/PID/exe` hash. Catches the failure mode where a prior install replaced the binary on disk but never restarted the service (B13 in DOCUMENTATION.md).

### In flight 🟡

- **M4 — Clan Layer v1** (PRD §8, ~4 weeks part-time — "the iceberg"). Started 2026-05-27.
  - **Phase 0 done** (`dccd7fb` + `577daed`): `docs/clan-protocol.md` → v0.2 with 6 peer-review-surfaced edits (12-word BIP39 paste-key + HKDF, `recently_failed` definition, `target_member` in signed token claims + v1 honesty note, `/v1/messages` + `/clan/peer-add` rows, OQ-2 pulled into v1). Local-LLM peer-review driver + raw reviews at `scripts/m4-peer-review.py` + `scripts/m4-reviews/`.
  - **Phase A done** (`8b1c2fd`): cland module skeleton — `cmd/minti-cland/main.go` + `internal/{config,identity,state,auditlog}/`. Ed25519 keypair + UUIDv4 persist to `identity.json` (mode 0600); Clan state at `clan.json` (also 0600, holds `clan_key`) with atomic-rename writes; auditlog schema duplicated verbatim from `mcp-servers/internal/audit` (D-M4.6).
  - **Phase B done** (`990fbc2`): crypto + transport. `crypto/` package — self-signed X.509 from Ed25519 with SPKI `sha256:<hex>` pin; HMAC-SHA256 over canonical request `(METHOD\nPATH\nsha256(body)hex\nts\nnonce)` per spec §2.3; `KeyProvider` interface (Current + optional Grace) wired from day one per D-M4.11. `transport/` package — HTTPS server with auth middleware (±60 s window, replay-rejection via LRU-bounded nonce cache, 401 empty body + audit-log on every reject, grace-key acceptance during rotation, `HandleAnonymous` escape hatch for the future `/clan/join`); HTTPS client with `VerifyPeerCertificate` SPKI pin enforcement.
  - **Phase C-core done** (`0210239`): BIP39 paste-key + membership crypto core + CLI subcommands. `internal/bip39/` vendors the canonical bitcoin/bips english.txt (sha256 verified) + 12-word mnemonic encode/decode w/ checksum + case/whitespace normalisation. `internal/membership/Create()` founds a Clan; `PreJoinViaMnemonic()` does the joiner-side derivation. CLI gains `create`/`members`/`show`. **Founder + joiner derive byte-identical clan_keys** given the same mnemonic.
  - **Phase C-rest done** (`ef328f2`): HTTP endpoints (`/clan/{invite,join,welcome,members,leave,revoke}`) on top of the Phase B transport; `invite` uses HandleAnonymous (joiner has no key yet), the other 5 are HMAC-gated. `InviteStore` (single-use, TTL 60s..24h), `Service.{Welcome,Leave,Revoke,SweepZombies}` + 60 s zombie sweep ticker per spec §3.1. CLI `invite`/`join` (paste-key + token paths)/`leave`/`revoke`. End-to-end smoke proved founder + joiner pair formation.
  - **Phase D done** (`73ecb2a` foundations + `d3f26a3` wiring + 2 prereq fixes): three foundation packages — `peers/` (Registry with candidate vs member-keyed stores, AdFresh/Live predicates, peer-add DoS guardrails 100-cap + 10/60s per-origin + TCP pre-dial), `scores/` (rubric loader + ReasoningScore + SystemScore + sliding-5-min RecentFailures), `probe/` (Linux + Windows hardware probe with EMA-smoothed SHA-256 CPU benchmark, nvidia-smi VRAM, runtime-adapter capabilities client). Then `discovery/` (grandcat/zeroconf with 1s debounce + clan_id filter + foreign-clan WARN + IsActive gating + Candidate-only emission per qwen3.6 1A) and `advertise/` (30s tick + 5s initial-delay + Bump rate-limited + per-peer failure record). Three new HTTP endpoints — `/clan/{advertise,peer-add,peers}` — registered behind the existing HMAC middleware. Two new CLI subcommands (`peer-add`, `peers`).
    - **2 prereq fixes** surfaced + landed in this commit: (1) the daemon's TLS server now uses the **shared** `clan_cert` priv key (founder's Ed25519) — Phase C latently used each member's identity priv, which only worked one-way; (2) **Advertisement.LANAddress** added so receivers register the sender's listen port, not the incoming TCP socket's ephemeral source port.
    - **End-to-end smoke** (Windows, two state dirs on 127.0.0.1:17980 + :17981): founder creates → daemons run on both sides → `peer-add` from founder → within 6s both `peers` commands show each other as `ad_fresh=true` with `system_score=66`.

  - **Phase E done** (2026-05-27, see commit log): leader-lease election engine. New `cland/internal/election/` package — `state.go` (in-memory term/orch/lease + history ring), `engine.go` (single-goroutine ticker driving heartbeat emit + election trigger + candidate selection + quorum + ⌈N/2⌉ accept counting), `handlers.go` (`POST /clan/heartbeat`, `GET /clan/orchestrator`, `GET /clan/election/history`, `POST /clan/pin`). Spec §5.2 cadence (HEARTBEAT_INTERVAL=2s / LEASE_DURATION=8s / FAILOVER_GRACE=6s / ELECTION_TIMEOUT=1s) configurable via `cfg.Election`. State extensions: `Clan.CurrentTerm` + `Clan.CurrentOrchestrator` + `Clan.PinnedOrchestrator` persisted via the existing atomic-rename path. New CLI: `pin --self|--clear`, `orchestrator`, `election-history`.
    - **7 peer-review-driven design fixes** baked in: R1 zombie-leader gate (heartbeat skipped if runtime probe unhealthy), R2 persist-on-change only (LeaseExpires in-memory — eliminates 1-write-per-2s I/O storm), R3 full spec §5.3 payload schema (`active_roster` + `reasoning_score`), R4 local lease tracking (ignore wire `lease_until`), R5 active-only quorum (drop `admitted`), R6 startup grace (no election in first FAILOVER_GRACE — handles post-restart heartbeat-catch-up), R7 50-150 ms random backoff. Plus a step-down behavior (Orchestrator vacates when `selectCandidate` now prefers another, enabling pin propagation), a higher-term-bypasses-anti-spoof rule (Raft convention; without it a restarted daemon split-brains against a survivor that ran a successful failover), and a `HeartbeatSeen` flag in `peers.Member` (so a once-heartbeating-now-dead peer drops out of candidate selection instead of being preserved by the AdFresh fallback).
    - **3 peer-review artifacts** at `scripts/m4-reviews/phaseE-{qwen3.6_latest,deepseek-r1_32b,gemma4_31b}.md`. gemma4 returned the only blocker (E1 zombie leader → R1), qwen3.6 caught the active-only quorum point (R5), deepseek corroborated. Driver at `scripts/m4-phaseE-review.py`; raw input snapshot at `scripts/m4-reviews/phaseE-input.txt`.
    - **16 unit tests green** in `internal/election/` covering lone-member election, AdmittedAt tiebreaker, pin override, multi-pin tiebreaker, anti-spoof (same-term reject) + higher-term bypass, term replay, lease expiry trigger, active-only quorum, R7 backoff bounds, persistence-omits-LeaseExpires, zombie-leader gate, startup grace, foreign clan_id reject, and step-down on preferred-elsewhere.
    - **End-to-end smoke** (`scripts/m4-phaseE-smoke.ps1`): two daemons on `127.0.0.1:17980` + `:17981`, paste-key join, advertise exchange, election commits with both sides agreeing on Orchestrator at term=1, kill the Orchestrator, survivor takes over at term=2 within 14 s, restart the killed daemon → it loads persisted term=1 + then accepts the survivor's higher-term heartbeat (term=2). Election-history surfaces the failover reason. `MINTI_CLAND_FORCE_HEALTHY=1` env-var hatch documented in main.go + advertise.go lets the smoke run without minti-runtime alongside; production deployments leave it unset and the R1 gate enforces.

  - **Phase F done** (2026-05-27, see commit log): routing layer. New `cland/internal/router/` package implements spec §6.1: every cland process exposes `POST /v1/chat/completions` + `POST /v1/messages` + `POST /api/chat` on its HMAC transport. The Orchestrator self-routes via the loopback fast path (D-M4.10) — straight to `http://127.0.0.1:7780`, no TLS, no HMAC. Non-Orchestrators proxy transparently to the current Orchestrator over HMAC HTTPS using the same `transport.Client` the election engine already uses. Streaming pass-through preserves SSE (OpenAI/Anthropic) + NDJSON (Ollama) chunk boundaries via explicit `http.Flusher` writes. Inbound HMAC headers are stripped on the upstream hop (the Orchestrator re-signs when forwarding to a peer; runtime-adapter doesn't need them). Hop-by-hop headers (RFC 7230 §6.1) also stripped.
    - **No orchestrator-yet → 503**; **peer-addr-unknown → 503** with `X-Minti-Expected-Orchestrator` header pointing at the persisted current; **local-runtime-down → 502**. Decorated with `auditlog` entries for every accept and deny.
    - **8 unit tests** in `internal/router/`: no-orchestrator reject, self-orchestrator self-routes, local-runtime-down → 502, not-orchestrator peer-proxies, peer-addr-unknown → 503 + redirect header, SSE pass-through, HMAC-quad stripped on forward, real-HTTP roundtrip against `httptest.NewServer` fake runtime.
    - **Phase E regression smoke** re-run with router wired in: election, failover, term bump, restart-accept all pass — router didn't disturb anything.
    - **Deferred**: a real chat-completion end-to-end smoke needs runtime-adapter + Ollama wired into the daemon test rig. The router proxy logic is fully covered by the unit suite + the Phase E regression; the smoke deferral is about test-harness scope, not correctness gaps. Worker routing (`system_score × (1 - load)`) is plumbed in the package but unused until `/v1/embeddings` / `/v1/vision` endpoints land in runtime-adapter.

  - **Phase G done** (2026-05-28, see commit log): cross-Clan tool execution. New `cland/internal/toolexec/` package serves spec §7.1's `POST /mcp/execute`:
    - **Token** (`token.go`) — request_id / origin_member / target_member / tool / args_hash / approved_at / exp / sig. Canonical = LF-joined fields; HMAC-SHA256 over canonical with the shared `clan_key` via `crypto.KeyProvider` (current + grace, so rotation doesn't break in-flight tokens). `args_hash = sha256(raw_args_bytes)` — origin and target see byte-identical args; we never re-marshal, so JSON key-ordering can't break verification.
    - **Executor** (`executor.go`) — wire spec `"mcp-recon.nmap_scan"` parses to server `minti-mcp-recon` (binary at `cfg.MCP.BinariesDir`) + tool `nmap_scan`. Spawns the subprocess via the official `github.com/modelcontextprotocol/go-sdk` v1.6.1 (same SDK mcptest uses), lists tools to validate the name, calls the tool, normalises `[]Content` blocks into a JSON-safe `ExecResult`. `exec.CommandContext` + `DefaultExecTimeout=5 min` cap runaways.
    - **Replay** (`replay.go`) — bounded in-memory `request_id` cache (LRU-style sweep when full, per-entry TTL). Default 10 000 entries × 15 min — covers the worst-case `DefaultMaxTokenLifetime=10 min` token window.
    - **Handler** (`handlers.go`) — strict order: (1) args-hash equality (catches arg tampering after sign), (2) HMAC verify under current OR grace key, (3) target/exp/approved_at claims, (4) replay (only AFTER all prior checks pass — so attackers can't pollute the cache cheaply), (5) spawn. Each failure mode emits a distinct 401/403/502 + a structured `auditlog` entry with a stable reason string.
    - **mcp-servers stub updated** — `permission.VerifyCrossClanToken` retains its signature but now documents that real verification lives in `cland/internal/toolexec`; nothing in mcp-servers calls the function, the executor's local `policy.deny_tools` is enforced inside each MCP server binary just as it was in M2.
    - **v1 honesty note** baked into the package doc-comment: the shared `clan_key` HMAC proves the token was issued by *some* current Clan member, not specifically by the claimed `origin_member`. A malicious insider could forge a token claiming a different origin. The "permission prompt on origin" guarantee is UI control, not cryptographic. v2 per-member keypairs close this gap.
    - **13 unit tests** in `internal/toolexec/`: sign/verify roundtrip, tamper-detection (Tool + RequestID bit-flip), wrong target → 403, expired → 401, future approved_at → 401, replay → 401, foreign clan_key → 401, args-tamper-after-sign → 401, grace-key acceptance during rotation, happy-path with audit, executor failure → 502, namespace parsing, replay cache TTL eviction.
    - **Phase E smoke regression-clean** with toolexec wired in.
    - **Deferred**: a real end-to-end smoke (origin daemon mints token → POSTs to target daemon → real MCP server spawns → returns tool output) needs a built minti-mcp-* binary on the smoke rig. The 13 unit tests + the M2 mcptest pattern (which has run real recon scans end-to-end) cover the executor path; the deferral is about smoke-test harness scope, not correctness. Local cland-side policy intercept (the spec's `403 policy_deny` before spawn) is deferred — the MCP server's own `deny_tools` enforcement still applies, but as an `IsError` result rather than a clean 403.
  - **Phase H-1 done** (2026-05-28, see commit log): key rotation 2PC. New `cland/internal/keyrotate/` package:
    - **types.go**: ProposeRequest / CommitRequest / AbortRequest / AckResponse / RotateResult wire shapes. PROPOSE_TIMEOUT=60s, MemberRevertAfter=90s (1.5×), DefaultGraceDuration=5min per spec §8.1.
    - **store.go**: in-memory `ProposeStore` — at most one pending propose; `ErrAlreadyPending` on collision; idempotent re-put for same id; `SweepExpired` clears pending after MemberRevertAfter (defends against an Orchestrator crashing mid-rotation).
    - **member.go**: `MemberHandler` exposes `POST /clan/rotate-key/{propose,commit,abort}` behind the existing HMAC middleware. Commit pops the pending key and invokes `KeyProvider.Rotate(newKey, graceDur)` — `crypto.KeyProvider`'s existing rotation mechanics (grace = old key, current = new) handle the 5-min in-flight tolerance natively.
    - **coordinator.go**: Orchestrator-side 2PC. Generates 32-byte new key + propose_id, broadcasts PROPOSE to all active roster members in parallel, collects ACKs within PROPOSE_TIMEOUT. If any peer 4xx/5xx/timeout → broadcast ABORT to everyone who ACKed (so they don't have to wait MemberRevertAfter to revert) + skip self-rotate. If all ACK → broadcast COMMIT + self-rotate. Lone-orchestrator special case (no peers): self-rotates trivially.
    - **trigger.go**: `POST /clan/rotate-key` (the local-CLI trigger) checks Orchestrator-status gate first, returns 503 + "not orchestrator" if not. Routes to coordinator.Rotate().
    - **CLI**: `minti-cland rotate-key` — POSTs to local daemon, prints commit/abort result.
    - **Daemon wiring** in main.go: also starts a background sweep goroutine that calls `proposeStore.SweepExpired()` every PROPOSE_TIMEOUT (60s) — covers the orchestrator-crash scenario.
    - **10 unit tests** in `internal/keyrotate/`: store put-accepted / put-different-id-409 / put-same-id-idempotent / sweep-expired / take-matches-id; member propose happy / commit-without-propose-409 / commit-after-propose-rotates / abort-clears; coordinator happy-all-ack / one-peer-fails-aborts (no self-rotate) / lone-orch / network-error-treated-as-failure.
    - **Phase E smoke regression-clean** with keyrotate wired.
    - **Deferred to Phase H-2**: revocation gossip. `/clan/revoke` already exists from Phase C (writes `revocations.json`, flips roster entry to "revoked", `peers.Registry.SetRevocations` consults the list on candidate binding). What's missing is *cross-daemon propagation* — Phase H-2 will piggyback revocations digest on heartbeats so a partitioned-then-healed node syncs.

  - **Phase H-2 done** (2026-05-28, see commit log): revocation gossip via heartbeat digest. New `cland/internal/revocations/` package:
    - **state.Revocations.Digest()** — sha256-hex of sorted member_ids, LF-joined. Stable across permutations; empty list yields a fixed value (sha256 of empty input); per-entry timestamps + reasons are not in the digest (only the SET of revoked members matters for membership filtering).
    - **state.Revocations.Merge(other)** — union, dedup by MemberID, local metadata wins on conflict.
    - **election.Heartbeat** gains `revocations_digest` (emitted by `Engine.emitHeartbeats` + `runElection` announce; non-blocking, ignored by Phase E receivers that haven't been recompiled).
    - **revocations.Syncer.MaybeSync(senderID, theirDigest)** — called from `election.Handlers` after the heartbeat passes lease/term/anti-spoof. Compares local digest to theirs; on mismatch + unknown peer addr → log + skip; on mismatch + known addr → GET `https://<addr>/clan/revocations` via the HMAC-stamping `transport.Client`, merge into local store, refresh `peers.Registry.SetRevocations` so the next `BindMember` consults the updated set. Per-peer in-flight dedup prevents concurrent fetches when many heartbeats arrive close together.
    - **revocations.Handler** registers `GET /clan/revocations` behind the existing HMAC middleware.
    - **8 unit tests** in `internal/revocations/`: digest permutation-invariant, empty digest has stable value, empty-digest MaybeSync skips, matching digest no-op, mismatched digest fetches + merges + refreshes registry, unknown-peer-addr doesn't panic, fetch error preserves local, idempotent on repeat, GET returns correct shape, GET on empty store returns empty list.
    - **Phase E smoke regression-clean** with revocations wired.
  - **Phase I done** (2026-05-28, see commit log): install + systemd + Makefile + minti-fetch. End-to-end smoke-validated on the real `minti-dev` Linux Mint 22.3 VM:
    - **Makefile**: VERSION bumped to `0.1.0-M4`; new `cland` (native) + `cland-linux` (cross-compile) targets parallel to runtime; `cland` joined `all:`; `clean` extended.
    - **`cland/systemd/minti-cland.service`**: hardened from the runtime unit — `ProtectSystem=strict`, `ProtectHome=true`, `PrivateTmp=true`, `NoNewPrivileges=true`, `LockPersonality=true`, `MemoryDenyWriteExecute=true`, `RestrictRealtime=true`, `User=minti`, `Group=minti`. `ReadWritePaths=/var/lib/minti/cland /var/lib/minti`. **`IPAddressDeny=any` deliberately omitted** (cland needs LAN peer traffic on :7777; documented). `Restart=on-failure` + `RestartSec=3`.
    - **`install/install.sh`**: cland deployment block inserted between MCP and opencode sections. Reuses M3 B13 binary-hash-vs-`/proc/PID/exe` restart pattern verbatim. Reuses config-without-clobbering pattern for both `cland.yaml` and `reasoning-scores.yaml`. State dir mode 0700 owner minti:minti (holds clan_key). Final summary line adds `Cland:`; next-steps surfaces `minti-cland create`.
    - **`branding/minti-fetch`**: legacy static-file Clan section (which nothing ever wrote) replaced with live `minti-cland show` + `minti-cland orchestrator --json` calls. Surfaces clan_id + role + member count + Orchestrator + `(self)` tag + term. Falls back to `(sudo for details)` on permission error (system daemon's clan_key is unreadable by regular users) and `(not affiliated)` pre-Clan. 1 s timeouts on all daemon calls so a hung daemon never blocks fetch.
    - **1 operational fix surfaced + landed**: the daemon under `ProtectHome=true` couldn't reach `/home/minti/.minti/audit.jsonl` (the default auditlog path) — process crash-looped on startup. Fix: `Environment=HOME=/var/lib/minti/cland` in the systemd unit redirects the per-user audit log into the already-writable state dir. Zero code change; works because the `minti` system user is `--no-create-home` (the real /home/minti was never going to exist anyway).
    - **Verified end-to-end** in the VM: install completes cleanly (5/5 MCP + runtime + cland + opencode), service goes `active`, `minti-fetch` correctly shows `(not affiliated)` pre-Clan and `Clan: <id> role=founder members=1 Orch: <self> (self) term=1` post-`create`. HTTPS listener on :7777 binds, returns 401 to unauthed curls (HMAC auth wired). Re-running install.sh is idempotent: configs preserved, binary hash matches → no restart. Journald shows clean election cycle ("election engine started" → "election won term=1 accepts=1 quorum=1").
  - **Next:** **Phase J** — clone `minti-dev → minti-dev-2` (host-only NIC 2 for mDNS), run the 16-test acceptance gate. M4 done after J.

  - **Phase J done** (2026-05-28, see commit log): 3-node testbed validation — VM-A (minti-dev) + VM-B (minti-dev-2, cloned) + Windows 11 host running `minti-cland.exe` directly. Cross-OS Clan consensus demonstrated end-to-end.
    - **Peer-review pass first** (J1-J7 fixes folded in before any execution): `scripts/m4-phaseJ-review.py` ran gemma4 + qwen3.6 (`think:false`) + deepseek-r1; all 3 reviewers caught 2 real blockers (identity collision after VM clone, SSH host-key collision) + 1 ordering bug (test 10 → 14 quorum collapse) + Windows process-lifecycle concerns. Triage + plan v2 captured in `~\.claude\plans\hello-what-do-you-valiant-ullman.md`; 4 reviewer findings explicitly overruled with reasons (PROPOSE_TIMEOUT bump, zombie-leader on restart, election-split-sim, full Windows Service NSSM).
    - **`scripts/m4-setup-second-vm.ps1`** automates the VBoxManage clone + host-only NIC 2 on both VMs + DHCP enable + port-forwards + shared-folder re-add + J1+J2 post-clone hygiene (wipe inherited `/var/lib/minti/cland/*` + regen `/etc/ssh/ssh_host_*`). Idempotent; PS5.1 stderr quirks documented in comments.
    - **Windows-as-Clan-member** via the existing `cland/minti-cland.exe`: state dir `%LOCALAPPDATA%\MINTI\cland`, `listen.address: "192.168.56.1"` (the host-only adapter), `Start-Process -WindowStyle Hidden -PassThru` per J4 (PID-tracked for alive-checks), `$env:MINTI_CLAND_FORCE_HEALTHY = "1"` to bypass the R1 runtime gate. **One firewall rule needed**: `New-NetFirewallRule -Name minti-cland -Direction Inbound -LocalPort 7777 -Protocol TCP -Action Allow -Profile Any` (admin). Documented in commit message.
    - **Two real protocol findings surfaced during validation**:
      1. **VMs advertised their NAT IP (10.0.2.15) instead of host-only IP** — `listen.address: "0.0.0.0"` resolved to the first non-loopback (NAT comes before host-only in NetworkManager iface order). Fix: set explicit `listen.address: "192.168.56.X"` per VM. Operational, not code; documented for future installer guidance.
      2. **Admitted members never promoted to "active"** — spec §3.1 says promotion happens on first capability advertisement, but our membership Service writes "admitted" on welcome and no code path ever flips them. The R5 quorum filter (active-only) from Phase E peer-review then computed N=1 per node, so every node self-elected and consensus broke at 3 nodes. **Code fix in `engine.go quorum()`**: count both "active" + "admitted" (with comment documenting the trade-off). **Phase H-3 follow-up**: wire the promotion through the /clan/advertise receive path + gossip the promotion via heartbeat.
    - **End-to-end demonstration** (Pass 3-ish — abbreviated from the full 16-test gate): all 3 nodes joined Clan `5725d958...` via paste-key, cross peer-added, converged on Windows as Orchestrator at term=19 (Windows reasoning_score 50 via FORCE_HEALTHY > VMs' 35 from real ollama llama3.2:3b rubric). Killed Windows orchestrator → VM-B took over at term=22 within 16 s; both VMs agreed on the new winner. Election-history showed clean term progression (18 → 19 → 20 → 21 → 22).
    - **Deferred to follow-up**: (a) Phase H-3 admitted→active promotion + gossip, (b) full 16-test PowerShell validate script (`scripts/m4-validate.ps1`) — Pass 4 in the plan, time-boxed out. Core functionality validated; the gate adds operational coverage but not fundamental correctness. Also deferred: VM NAT port-forward repair (host-only IPs work fine for everything Clan-related; NAT was only needed for VirtualBox-style "ssh -p 2222" which broke after adding nic2 — moved to host-only IPs throughout).

  - **Phase E done** (2026-05-27, see commit log): leader-lease election engine.

- **M5 — Cross-platform Clan Agent (Windows + macOS)** (2026-05-28, three commits: `25e1cf1` `4e5373b` `dbd6e10`). PRD §8 line 481 verbatim: "Same `cland` binary cross-compiled; tray app via Fyne." User-confirmed scope: full Windows + macOS together, service-only (tray deferred to M5.1), Windows service via NSSM. Pre-coding 3-LLM peer-review pass on the plan; 9 consensus items folded before the first character was written. Plan + reviews at `~\.claude\plans\synthetic-popping-owl.md`.
  - **M5-A done** (`25e1cf1`): foundations. `cland/internal/probe/probe_darwin.go` (`SysctlRaw` + `binary.LittleEndian.Uint64` for RAM + kern.boottime; `pmset -g batt` with `hasBattery` Once-cache + index-safe parse + "No batteries"/"not present" handling). `cland/internal/probe/probe_windows.go` battery placeholder replaced with `GetSystemPowerStatus`. `cland/internal/config/paths_unix.go` + `paths_windows.go` exporting `DefaultStateDir/AuditLogPath/ConfigPath/RubricPath/MCPBinariesDir`; Windows side uses `golang.org/x/sys/windows.GetCurrentProcessToken` + SID compare against `S-1-5-18` for LocalSystem detection (NOT the broken "LOCALAPPDATA empty" heuristic — peer-review item 5). `config.go Default()`, `auditlog.go DefaultPath`, `main.go` 14 config-default sites + help string all delegate to the helpers. `srv.Shutdown` budget dropped 5s → 3s (peer-review item 7: Go 3s + NSSM 5.5s stays under SCM "Not Responding" threshold). Top-level Makefile gained `cland-windows`, `cland-darwin-{amd64,arm64}`, `cland-all-platforms` with `-trimpath -ldflags="-s -w"` (~30% smaller binaries). Verified: vet clean on linux/windows/darwin, `go test ./...` 18 packages green, 4-platform cross-compile correct file types, Win32 ground-truth battery probe matches cland reading.
  - **M5-B done** (`4e5373b`): Windows NSSM-managed service. `cland/windows/nssm/install-cland.ps1` (`icacls` strict DACL on state dir; explicit `win64/nssm.exe` from the 2.24 zip with SHA-256 sidecar verified at install time — peer-review item 2; `-FirewallProfile Private,Domain` default with operator override — peer-review item 1; `AppStopMethodConsole 5500`; Defender Folder-Exclusion guidance printed at end — peer-review item 8). `uninstall-cland.ps1` preserves state by default, `-Purge` wipes. `build-zip.ps1` cross-compiles + downloads NSSM (curl.exe primary with retry, Invoke-WebRequest fallback — nssm.cc is famously flaky; in this session we hit a 503 and the script recovered cleanly on the second try). `cland.yaml.windows.example` with explicit `state.dir:` so the helper's LocalSystem fallback is belt-and-braces. README operator quickstart + security model + Defender + network-profile + NSSM tuning reference. Makefile `cland-windows-zip` target. `.cache/` gitignored. **Live install verified on the daily-driver Windows 11 host**: NSSM SHA-256 verification fired, DACL exactly `Administrators:(OI)(CI)(F)` + `SYSTEM:(OI)(CI)(F)` no inheritance, service log shows `version=0.2.0-M5` + correct config/state/identity wiring, identity persisted at `%PROGRAMDATA%\MINTI\cland\identity.json`. **One real-world finding surfaced**: the Phase J foreground `minti-cland.exe` from last week (PID 23752, bound to `192.168.56.1:7777`, ESTABLISHED to both VMs) was still running and would have blocked the new service. Stopped during verification — M5-D's validate script now has an explicit "no stray foreground processes" preflight. The service is **currently installed and Running** on the host in pre-Clan state; manually uninstall with `powershell -ExecutionPolicy Bypass -File C:\Temp\m5b-cland-install\uninstall-cland.ps1`.
  - **M5-C done** (`dbd6e10`): macOS launchd daemon. **No Mac available during M5**; live install deferred to M5.1 dogfood. Second targeted 3-LLM peer-review pass on the dscl + launchctl recipes specifically; 6 items folded: (1) `HiddenSystemUser=1` alongside `IsHidden=1` for 10.13–14.x coverage, (2) `xattr -d com.apple.quarantine` after install, (3) `plutil -remove UserName` instead of fragile sed range delete in the per-user variant, (4) dropped `if [...] || true` thinko around the bootout call, (5) UID scan range bumped 270→300 to clear Apple's third-party convention, (6) state-check broadened to any `launchctl print` recognition rather than `state = running` specifically (version variance). `com.minti.cland.plist`: `Label`, `ProgramArguments`, `RunAtLoad=true`, `KeepAlive={SuccessfulExit:false}`, `ThrottleInterval=5`, `UserName=_minti`, `EnvironmentVariables.HOME=<state_dir>` (mirrors the Linux unit's HOME redirect; `_minti` has `NFSHomeDirectory=/var/empty`), `ProcessType=Background`. `install-cland.sh` system branch creates `_minti` at lowest free UID 300–499, drops binary at `/usr/local/bin/`, state at `/Library/Application Support/MINTI/cland` with `chmod 0700 chown _minti:_minti`, plist into `/Library/LaunchDaemons/` with `root:wheel 0644` + `plutil -lint`, bootstraps via modern `launchctl bootstrap system <plist>` with `load -w` fallback. Per-user branch generates a UserName-stripped plist on the fly, bootstraps into `gui/<uid>`. `uninstall-cland.sh` mirrors; `--purge` deletes state + `_minti` account. `build-tarball.sh` cross-compiles + stages both archs + tarballs (uses `tar`, falls back to `python3 tarfile` for Git-Bash-on-Windows). `cland.yaml.darwin.example` + README (10-yr-old-Mac performance expectations, arm64 vs amd64, Gatekeeper workarounds, Application Firewall first-run). Makefile `cland-darwin-tarball` target. **Verified without a Mac**: `plistlib` parses the plist (all 11 expected keys present), `bash -n` syntax clean on all 3 .sh files, both Mach-O binaries produced (`amd64` + `arm64`), tarballs assembled with correct 8-file layout.
  - **M5-D scripted, live run deferred** (this session): `scripts/m5-phaseD-validate.ps1` written + syntax-checked. Wraps the Phase J 3-node scenario but assumes Windows is service-managed: service preflight (no stray foreground processes), mDNS cold-boot test (restart-service + sleep + query peers from both VMs over SSH), firewall scoping check + disable/enable toggle, identity.json DACL audit (skips if non-elevated), election with Windows as a candidate (kill Linux Orchestrator → verify survivor takes over → restart). 24h Defender soak is OUT OF SCOPE for this script — separate poll-script work for M5.1. **Live run requires**: both VMs warmed up + SSH-reachable on host-only IPs, Windows service already joined to the Phase J Clan (currently the service is in pre-Clan idle; would need a `join` or `create` first), an elevated PowerShell session for the DACL audit branch.

- **M6-content done** (2026-06-06, this session): addon packs (DLC) — Hermes 3 + Mistral + offline Simple Wikipedia. Reuses M2's `packs/recon/` debhelper-compat-13 metapackage skeleton but extends it for two new content kinds that aren't apt artifacts: Ollama model tags + Kiwix ZIM blobs.
  - **`pack-manager/cmd/minti-pack-fetch`** — new Go single-file helper (~9 MB, stdlib only). Reads `/usr/share/minti/packs/<name>/manifest.json`, dispatches by `kind`: `ollama-model` shells to `ollama pull <tag>` (idempotent), `kiwix-zim` does HTTP Range-resumable download with SHA-256 verify + 1.25× free-disk precheck + atomic rename. Writes `/var/lib/minti/packs/<name>.installed` state markers. Honours `MINTI_PACK_NO_FETCH=1` env-var: writes marker, skips download, lets `apt install` finish fast on metered links. Split build tags (`disk_linux.go` / `disk_other.go`) keep native dev builds compiling on Windows.
  - **Three new packs** under `packs/{hermes3,mistral,wiki-simple}/`. Each: `debian/{control,rules,changelog,copyright,source/format,install,postinst,postrm}` mirroring recon's structure, plus a `manifest.json` + agent-facing `skill.md`. wiki-simple adds `debian/dirs` (for `/var/lib/minti/wiki`), `debian/prerm`, and a `systemd/minti-kiwix-serve.service` unit (loopback only, hardened with the same `ProtectSystem/PrivateTmp/MemoryDenyWriteExecute` profile as `minti-cland.service`). Postinst calls `/usr/local/bin/minti-pack-fetch` (the helper ships with the base install, not as a separate .deb at v0.1 — documented in each pack's Description).
  - **`minti-mcp-wiki`** — new MCP server at `mcp-servers/cmd/mcp-wiki/main.go`. Two tools: `wiki_search(query, limit)` queries kiwix-serve's OPDS feed (falls back to HTML scraping for older versions), `wiki_get(path)` fetches an article and HTML-strips. Policy gate `mcp.wiki.deny_tools` added to `policy.MCPPolicy`; permission router extended in `permission.Check()`. Reads `/etc/minti/wiki.yaml` for the kiwix-serve base URL (override via `$MINTI_WIKI_BASE_URL` for tests). Mirrors the mcp-recon framework exactly.
  - **Default-model fallback** in `runtime-adapter/internal/server/server.go`: new `s.resolveModel(ctx, requested)` helper. If caller omits `model`, prefers `hermes3:8b` → `mistral:7b` → first model returned by the backend. Wired into both the OpenAI handler (`chat.go`) and the Anthropic handler (`messages.go`); Ollama passthrough left strict (callers always specify there). Five lines of logic; existing tests still pass.
  - **Three small touch-ups**: install.sh epilogue (lines 445–474) now mentions Hermes/Mistral in the model-pull suggestions and lists the three addon packs in a new step 5; `install/opencode.config.example.json` adds a `mistral:7b` row + marks `hermes3:8b` as conceptual default; `branding/minti-fetch` reads `/var/lib/minti/packs/*.installed` and surfaces an `Addons:` line (verified live).
  - **Makefile**: new `PM_DIR=pack-manager` module, `pack-fetch` + `pack-fetch-linux` targets, three new `pack-{hermes3,mistral,wiki-simple}` targets built via a shared `build-pack` macro. `mcp-wiki` added to `MCP_SERVERS`. The `build-pack` macro also `chmod -x`'s `debian/install`/`dirs`/`control`/`changelog`/`copyright` because vboxsf shared mounts mark everything executable, which otherwise confuses dh_install — see Phase E findings below.
  - **End-to-end smoke** in the `minti-dev` VM (host-only DHCP didn't recover post-restart, so this session used the NAT port-forward `127.0.0.1:2222`): all three .debs build clean; `sudo MINTI_PACK_NO_FETCH=1 dpkg -i minti-pack-hermes3_0.1.0-M6_all.deb` lands the marker + skill.md + manifest.json correctly, postinst runs and prints the deferred-fetch hint; `minti-pack-fetch -list` reports `hermes3 ollama-model installed 2026-06-05`; the updated `minti-fetch` shows `Addons: hermes3`; `dpkg --purge` removes the marker cleanly. wiki-simple unpack lands the systemd unit at `/lib/systemd/system/minti-kiwix-serve.service` correctly but the **smoke run used `dpkg -i` which doesn't resolve Depends** — production deploy needs `apt install ./minti-pack-wiki-simple_*.deb` (or pre-install `kiwix-tools`) to pull the dep. **A real Ollama pull was deliberately NOT run** (Hermes 8B is ~5 GB; deferred until needed); the `MINTI_PACK_NO_FETCH=1` path exercises everything the unsetup path does except the actual download.
  - **3 Phase E findings worth recording**:
    1. **vboxsf is mode-locked.** Files on the shared folder are reported with all bits set; `chmod -x` is a no-op. dh_install then treats `debian/install` as an executable config script and chokes on the bare `skill.md` line. Workaround: copy the pack out of the shared folder before building (`~/m6-build/<pack>/`). The Makefile's `build-pack` macro still calls `chmod -x` for the non-vboxsf case where it actually works.
    2. **debhelper `--with systemd` is deprecated.** Compat 11+ auto-loads the systemd sequence — `dh $@ --with systemd` errors out with "no longer provided". Fix: bare `dh $@`; dh_installsystemd still runs automatically when a `.service` file is present under `lib/systemd/system/`.
    3. **`Description-prereq:` is not a real control field.** dpkg-deb rejected it. Fold prerequisite notes into the regular `Description:` paragraph (used "Requires /usr/local/bin/minti-pack-fetch from the base MINTI install").
  - **Follow-ons for a future M6 commit**: ~~package `minti-pack-fetch` itself as a .deb~~ ✓ landed in M6.1 (2026-06-06); ~~deploy `minti-pack-fetch` from `install.sh`~~ ✓ landed in M6.1; ~~pin Kiwix ZIM SHA-256~~ ✓ landed in M6.1 (wikipedia_en_simple_all_nopic_2026-05.zim, 937 MB); add `minti-pack-hermes3-70b` + `minti-pack-mistral-nemo` + `minti-pack-wiki-en-top` + `minti-pack-wiki-en` follow-on tiers; consider building inside an `lxc-launch debian:13` to dodge vboxsf entirely; chmod -x the systemd .service files in the build-pack copy stage (currently dpkg warns "Configuration file ... is marked executable. Proceeding anyway.").

- **Dist D3 done** (2026-06-11, this session): **the two-door site — LIVE at https://minti-pi.vercel.app, with real downloads.**
  - **Operator decisions resolved this phase** (both asked + answered live): (1) artifact host = a new public **artifacts-only repo `emka21347-cyber/minti-releases`** (source stays private; doesn't foreclose publishing later); (2) explicit go-ahead for the Vercel production deploy (the auto-mode classifier correctly held it for consent).
  - **Release v0.4.0-D1 published**: checksums.txt + the 4 door-B artifacts (Windows zip 10 MB, Linux tarball 29 MB, macOS arm64/amd64 ~9/10 MB) at github.com/emka21347-cyber/minti-releases — release notes carry the verify-first + unsigned-binaries honesty and the macOS not-yet-run-on-hardware caveat. No ISO upload (none exists on this box; door A's copy says exactly that).
  - **`site/`**: single `index.html` (15.5 KB) + `assets/logo.svg` (reused brand asset) + `vercel.json` (static, CSP `default-src 'self'`, nosniff, no-referrer). Zero JavaScript, zero external resources beyond the GitHub release links, system mono stack, brand palette as locked (mint = single-focus, cyan = enumerables, no gradients). IA per the D0-folded plan: header → hero → door B primary (what-gets-installed table, 4 downloads with inline SHA-256s, 3-step installs per OS in `<details>`, uninstall + checksums + Ollama notes) → door A with the honest live-preview badge + ISO-not-uploaded-yet status box → **Known warnings** (D0 F2: SmartScreen/Gatekeeper steps, Defender, hardware expectations, one-open-port) → how-it-works strip → no-telemetry footer.
  - **T3 delegation round** (per the workflow rule): `scripts/d3-site-spec.txt` (tight spec, all load-bearing strings pinned) → qwen3.6 via t3-codegen.py (39 s, 15 KB) → **T2 line-by-line review caught 4 real HTML bugs**: a raw `<arch>` parsed as a tag and silently swallowed (would have rendered a broken filename), raw `&&` ×2, `height="auto"` (invalid), unescaped `&`/`>` in prose ×3. M4's lesson holds: T3 output ships subtle breakage; the review is load-bearing. Raw T3 artifact kept at `scripts/d3-site-t3-output.html`.
  - **Verification**: local serve (launch.json `site` config) → GET / + logo 200; **all 7 external URLs in the page return 200 live** (4 artifact downloads + checksums.txt + 2 repo links); tag-balance parser clean; all 4 hashes present; no JS; 19 KB total page weight. **Production verified**: https://minti-pi.vercel.app/ → 200, logo 200, both door headings + hash markers present. **Honest gap**: the preview-panel screenshot tool timed out repeatedly (tool-side renderer hang; server + page fine) — no archived desktop/360px screenshots; responsive layout is CSS-grid-below-760px + break-all hashes, code-reviewed but not pixel-verified this session (the Launch preview panel renders it live for the operator).
  - **D-milestone state after D3**: doors A+B are public. Door B fully real (download → install → workspace). Door A honestly labeled (live-preview ISO lands on the releases page when next built; disk installer = D4, build-VM-gated).

- **Dist D2 done** (2026-06-11, this session): **door B Linux + macOS.**
  - **Linux**: new `workspace/systemd/minti-workspace.service` — cland's hardening profile **plus** `IPAddressAllow=127.0.0.0/8 ::1/128` + `IPAddressDeny=any` (D0 F4) and **zero ReadWritePaths** (whole fs read-only to the unit + its shelled CLI — the Linux mirror of the Windows RX-only grant; a CLI write on an unconfigured box fails closed into demo mode). `install.sh` v0.4.0-D1: workspace deploy block (B13 stale-binary pattern), `first_existing` helper + tarball-layout candidates for every artifact (one script serves checkout + tarball), the Ollama block made **non-fatal on door-B installs but still fatal under `MINTI_CHROOT=1`** (D0 F6 — it was `set -e`-fatal before; a flaky ollama.com download aborted everything) + `MINTI_NO_OLLAMA=1` opt-out, summary gains Workspace/Ollama status lines, and the auto-open tail (30 s poll → `xdg-open` as `$SUDO_USER` with `--preserve-env=DISPLAY,WAYLAND_DISPLAY,XDG_RUNTIME_DIR`, headless ssh-tunnel hint, skipped in chroot). New `install/uninstall.sh`: consumers-first unit removal, binary cleanup incl. the opencode symlink (per-user ~/.opencode untouched), state + `minti` user preserved by default, `--purge` with printed warning, refuses chroot. `install/linux/build-tarball.sh` → `dist/minti-linux-amd64-v0.4.0-D1.tar.gz` (sha256 `4cb03b5f...`): 11 binaries + configs + 3 units + branding + install/.
  - **macOS**: `install/macos/` — `com.minti.{runtime,workspace}.plist` cloned from the peer-reviewed M5-C cland plist (runtime: `--config .../MINTI/runtime/runtime.yaml`; workspace: `-listen 127.0.0.1:8088` + **two load-bearing env vars**: `PATH` incl. /usr/local/bin — launchd daemons don't get it, LookPath would never find the CLI — and `HOME=/` so the CLI's non-root default resolves to `/Library/Application Support/MINTI/cland.yaml`, a **symlink the installer stages** at the root-default location → the darwin twin of the Windows LOCALAPPDATA trick). `install-minti.sh`: M5-C dscl `_minti` recipe verbatim, 3 binaries with quarantine xattr stripped from **all three** (D0 F2), per-service config staging, bootstrap-with-fallback loop, Ollama detect-or-guide (`/api/version` shape check), 30 s poll + `open` as console user, Application Firewall honesty; full per-user LaunchAgent branch (plutil-edited plists, $HOME paths, user PATH). `uninstall-minti.sh`: bootout + **delete all three plists** (D0 F9), preserve-state default, `--purge` removes state + `_minti`. `build-tarball.sh` → `dist/minti-macos-{arm64,amd64}-v0.4.0-D1.tar.gz` (sha256 `a2fdbf0c...` / `95efa03d...`).
  - **A real build bug caught by verification**: Go-on-Windows under Git Bash writes non-.exe outputs **without the executable bit** — and `chmod` is a no-op on MSYS/NTFS (mode faked from a content heuristic; ELF/Mach-O read back 0644). First tarballs shipped non-exec binaries that install.sh's `-x` candidate tests would have **silently skipped** on the target box. Fix: both builders tar via **python tarfile with an explicit mode filter** (root:root; dirs 755; bin/* + *.sh + branding 755; rest 644). Final archives verified: binaries `-rwxr-xr-x root/root`.
  - **Verification run**: `bash -n` clean on all 6 shell scripts; all 3 plists parse (python plistlib — caught an illegal `--` inside an XML comment in the workspace plist); all 3 tarballs build from this box with verified layout + modes + checksums; Makefile targets `minti-linux-tarball`/`minti-macos-tarball` added. **Honestly NOT verified**: the full Linux install/uninstall/re-install cycle needs the `minti-dev` VM (**off** — same deferral discipline as M5-D; run it when VMs are next up), the macOS recipes have **never run on Apple hardware** (M5-C posture, stated in the tarball README + future site copy), shellcheck not installed on this box (`bash -n` only), extracted-mode `-x` behavior provable only on a real Linux fs (archive-level modes verified). No Go code touched in D2 (D1's vet/test pass covers the modules).

- **Dist D1 done** (2026-06-11, this session): **door B Windows — the one-click full-stack installer, live-verified on the daily driver.** `install/windows/{install-minti.ps1,uninstall-minti.ps1,Install-MINTI.cmd,build-zip.ps1,README.md,configs/runtime.yaml.windows.example}` + Makefile (`VERSION=0.4.0-D1`, new `runtime-windows`/`workspace`/`workspace-linux`/`workspace-windows`/`minti-windows-zip` targets, workspace added to `GO_MODULES`). Artifact: `dist/minti-windows-amd64-v0.4.0-D1.zip` (sha256 `405c4140e9e7...`, NSSM from the M5-B-verified cache).
  - **Three services**: `Minti-Cland` (LAN :7777, LocalSystem — the live M5-B service **upgraded in place**, config + state preserved, no remove/re-add), `Minti-Runtime` (127.0.0.1:7780) + `Minti-Workspace` (127.0.0.1:8088) both under **`NT SERVICE\` virtual accounts** (D0 F1) via `sc.exe config obj=`; per-service NSSM copies; one icacls pass per state dir (SYSTEM+Admins F; runtime/workspace get M on their own state; workspace gets RX on cland's). **The F1 load-bearing trick**: the workspace service env sets `LOCALAPPDATA=%PROGRAMDATA%` + PATH+=cland dir — the shelled cland CLI (per-request `clan.Probe`) resolves the *system* state under a non-LocalSystem account (`paths_windows.go` falls back to LOCALAPPDATA), and the RX-only grant means a misbehaving CLI write fails closed into demo mode instead of corrupting state.
  - **Safety mechanics**: NSSM SHA-256 verified pre-use; stray-foreground-cland + foreign-7780/8088 preflights (refuse, never kill); ONE firewall rule (:7777 Private,Domain, program-bound); Ollama **detect-or-guide** (`/api/version` JSON shape-check per D0 F5, found 0.30.7 serving); 30 s workspace health poll; **de-elevated auto-open** via `explorer.exe` (D0 F3, fired live); uninstall preserves state by default + sweeps program-bound firewall rules (D0 F8); Defender guidance verbatim from M5-B.
  - **Live verification on the daily driver (elevated transcripts `C:\Temp\minti-d1-install\d1-{verify,cycle}.log`)**: (1) **upgrade-over-M5-B** — identity.json hash `544E5C9F...` + member `b2ecb5c4` + clan `5725d958` (role=joined) + cert pin byte-identical before/after; all 3 services Running under the intended accounts (Win32_Service StartName audited); ports 8088+7780 loopback-bound, 7777 on the configured LAN address; DACLs exact; workspace `GET /` 200 + **`GET /api/mesh` → `source=live configured=True`** under the virtual account (the F1 design proven end-to-end); runtime `/minti/health` 200. (2) **Re-run idempotent** — all configs "preserved", exit 0, same identity hash. (3) **Uninstall→reinstall cycle (no purge)** — services+binaries+rule gone, state preserved, reinstall exercised the **fresh-install code path** and returned the same identity + all services Running. Plus: `go vet`+`go test` green across all six modules (workspace newly in hygiene targets); all 3 scripts parse clean under PS 5.1; the shim's nested-quoting plumbing proven with a spaced-path stub.
  - **Honestly NOT tested**: `-Purge` (never on the daily driver — VM pass when VMs are next up); the shim's full double-click+UAC flow as a unit (its inner command proven non-elevated; its RunAs pattern proven by the verification launches themselves); the Ollama-missing guide prompt (Ollama is present here — missing-case logic is review-only); Defender ≥24 h soak (standing M5-B item); deep workspace UI interaction beyond `/` + `/api/mesh`.
  - **Two findings recorded**: (a) the daily-driver service was NOT pre-Clan as the M5-B entry believed — its ProgramData state holds Phase-J clan membership (member `b2ecb5c4`), distinct from the user-profile state (`e6c07875`) the old foreground process left in `%LOCALAPPDATA%`; the explicit `--config` invocation is the only honest way to read service truth non-LocalSystem. (b) UAC consent prompts auto-deny after ~2 min unattended — elevated verification needs the operator at the desk (took 3 launches + a phone push this session).

- **Dist D0 done** (2026-06-11, this session): the Distribution milestone's plan-and-review phase — **no code, by design; installers touch people's machines, so the plan went under the 3-LLM review bar before the first character of installer code.**
  - **Plan detail written into `docs/plans/distribution-roadmap.md`** ("D-milestone detail" section): the door-B common contract (3 services — cland :7777 LAN / runtime 127.0.0.1:7780 / workspace 127.0.0.1:8088 loopback-locked; ONE inbound firewall change; checksum-verified bundled binaries; state-preserving uninstalls; idempotent re-run as both upgrade and repair), per-OS flows (D1 Windows zip extending the proven M5-B NSSM pattern with upgrade-in-place over the live daily-driver service; D2 Linux tarball + workspace systemd unit + new uninstall.sh, macOS three-plist launchd extension of M5-C), the Ollama detect-or-guide strategy (Linux keeps its validated auto-install with a new non-fatal wrap + `MINTI_NO_OLLAMA=1`), the de-elevated auto-open flow, the D3 site IA (two doors, "what this installs" disclosure, Known-warnings section for the unsigned-binary reality), and D4 as a build-VM-gated pointer.
  - **3-LLM review ran** (`scripts/dist-d0-review.py`, outputs `scripts/m4-reviews/dist-d0-*.md`): qwen3.6 deep + specific (verdict: address LocalSystem/elevated-open/IPv6), gemma4 the dissenting voice + the unsigned-binary blocker (verdict: address signing honesty + site warnings), deepseek-r1 a weak round (no thinking trace, generic corroboration — noted in triage as a 2-strong-voice review).
  - **Triage at `scripts/m4-reviews/dist-d0-triage.md`: 10 folded (F1–F10), 8 overruled with reasons.** Headlines: F1 runtime+workspace drop LocalSystem for `NT SERVICE\` virtual accounts with explicit icacls grants while cland keeps the proven M5-B account (the panel's only split decision, resolved along the trust boundary — workspace compromise yields Clan-scoped clan_key read, not SYSTEM); F2 the site + macOS installer stop pretending unsigned binaries are seamless (3/3 consensus); F3 Windows auto-open de-elevates via `explorer.exe`; F6 a real install.sh find — the Ollama `curl|sh` step is fatal under `set -e` today, becomes warn-and-continue on door-B installs but stays fatal in ISO chroot builds. Overrules verified against code where checkable (e.g. workspace demo-mode stickiness: `clan.Probe()` execs per-request, clan.go:54).
  - **D1-readiness pre-verified**: workspace + runtime-adapter cross-compile clean for windows-amd64 (`go vet` + build, 6.3/7.2 MB) — the plan's headline assumption holds. Pre-flight ran before the review (31 GB VRAM free; the M5-B service + Ollama identified and left untouched).

- **Clan Memory M6 done** (2026-06-11, this session): the **scribe distillation duty** (spec §13.9) — the Karpathy move, live: activity in, curated memory out, a human between. ~700 LOC incl. tests. **This completes the Clan Memory plan (M0–M6).**
  - **`cland/internal/memory/scribe.go`**: debounced ticker (default 120 s) that runs on every member but distills only while `electionState.CurrentScribe() == self` (§13.8 closure-gated per tick). Three watched sources with in-memory high-water marks: workspace chat sessions (`*.jsonl` under `DefaultChatDir(clan_id)` — per-OS; env-overridable), the member's **own** audit log (the audit file stays §9.1-local; only distilled content gossips), and fresh findings in **open** research sessions (never re-distilling scribe output). **Boot = seed-to-EOF** — the first tick records marks without distilling, so a restart never dumps backlog on the model; a non-scribe keeps marks fresh so winning the role later doesn't either. Single-flight synchronous ticks are the v1 "skip if busy" guard; per-pass cap 5; the §13.9 **pending-proposal budget** (>200 own un-reviewed → pass skipped, marks still advance).
  - **The archivist call**: smallest resident model (`SmallestResidentModel` — parameter-hint heuristic, `0.5b < 1b < 8b`, env override) through the runtime-adapter's OpenAI surface, strict-JSON prompt, ≤1200 tokens. **`ParseProposals` is the §13.9 tolerant parser**: strips `<think>` blocks, tries a JSON decoder at every `[` (decoy brackets like "[1]" citations skipped), validates each entry (type whitelist — a model may not mint events/members/sessions; title required; caps), drops the invalid, caps at 5; garbage yields nil — never an error, never a graph write. Proposals land `status:"proposed"` + `source:"scribe"` + the §13.7 `contributes_to` edge; session references are validated against OPEN sessions (bad ones cleared); titles starting "session summary" on a session get the **deterministic §13.3 id** so repeated passes UPDATE one summary node instead of spawning twins.
  - **9 unit tests**: parser (clean/prose-wrapped/thinking-trace/decoy-bracket; invalid-drop + cap; pure-garbage → nil), model heuristic + env override, seed-tick never distills, no-activity tick never calls, fresh-chat distills exactly once and lands gated, non-scribe never calls or writes, garbage completion writes nothing, deterministic session-summary updates (rev ≥ 2, single node, single edge), pending budget skips the model entirely.
  - **`scripts/memory-m6-smoke.ps1` 6/6 PASS — REAL-LLM end-to-end on the host**: 1-node Clan (scribe == orchestrator == self, the §13.8 edge case; selection polls past the R6 startup grace — first run checked at 5 s and caught that), `runtime.base_url` pointed at **Ollama's OpenAI-compatible surface** (11434), scribe model gemma4:31b (no think-traces over this surface; qwen3.6 thinks), 8 s interval. Fed a synthetic workspace chat (a checksum incident + "make that a permanent rule") + an open research session with one finding. **The scribe proposed exactly the two durable memories a human would have kept**: `[decision] Mandatory SHA256 Verification for Content Packs` and `[finding] Kiwix 302 Redirect Strips Digest Header` — both gated at `proposed`, zero auto-actives (the human-gate assertion), explicit promote flipped exactly one with provenance intact (`source=scribe` survives promotion).
  - **Regressions clean after the wiring**: full `go test ./...` (19 packages, memory suite now 42 tests) + Memory M2 two-daemon gossip smoke 9/9 re-run.
  - **Honestly not yet run — the plan's final 3-node VM gate**: weakest VM node elected scribe distilling real workspace chats, graph converging in *each member's* workspace memory tab, session export → `create --from-blueprint` across real machines. Both VMs are powered off; every constituent behavior has been verified on the host across the M1–M6 smokes (gossip convergence incl. follower edits + failover, scribe election + re-selection + pin on a 3-daemon rig, blueprint round-trip + corrupt-byte rejection + joiner inheritance, real-LLM distillation behind the gate). The VM gate is operator-session work: bring up VM-A/VM-B, run the M2/M3 smokes cross-VM, then the workspace UI loop.

- **Clan Memory M5 done** (2026-06-11, this session): the **workspace memory tab** — the live graph-node visual of the Clan's research memory, in the locked vanilla-JS SPA. ~800 LOC (Go + JS/HTML).
  - **`workspace/internal/clan/memory.go`**: probe layer mirroring `clan.go`'s shell-out contract verbatim — `ProbeMemory` (`memory list --json` + scribe), `ProbeMemoryDigest` (the cheap 5 s change-poll: one CLI call, no graph transfer), `ProbeScribe`, and the mutation passthroughs (`MemoryAddNode` — id-present = update, so promote/dismiss ride the same verb; `MemoryLink`, `MemoryArchive`, `MemoryExportBlueprint` via temp file, `MemoryImportBlueprint`). Dev box without cland → an 11-node **demo graph** that exercises every visual: session hub, findings, decision, proposed scribe nodes, member ring, archived tombstone, system event.
  - **`server.go`**: `GET /api/memory{,/digest}`, `GET /api/scribe`, `POST /api/memory/{node,edge,archive,import}`, `GET /api/memory/blueprint` (download with Content-Disposition; `?session=&strip=1`). The four mutating POSTs are **enumerated in the package doc-comment for the PIN/bearer gate** (spec §13.6 workspace caveat) and ship loopback-only via main.go's default bind; blueprint export flagged for the same gate (it exports distilled chat content).
  - **`web/index.html`**: new rail icon (`ti-topology-star-3`, badge = pending-proposal count) + `p-memory` pane in the locked mock idiom (`el()`, `det` inspector card, `PN` nav map, brand vars, `fadein`/`.fl`, reduced-motion media query). **Force layout is the third T3 delegation** (qwen3.6, 17 s): deterministic golden-spiral seed (stable across reloads, no `Math.random`), O(n²) repulsion + edge springs + center gravity, cooling; T2 review verdict — sound (per-tick force accumulation instead of true momentum, which settles *better* for our freeze design), only dead vars trimmed. Layout runs **once per graph change, synchronously, then the SVG is frozen — no rAF loop at all, so CPU-idle-after-settle holds by construction**; entry animation is pure CSS. Render cap 250 newest with the "showing most recent" notice. Node visuals per brand: mint session hubs + decisions (single-focus), cyan findings/facts (enumerables), white skills, grey events, dashed-cyan member rings, dashed halo + reduced opacity for proposed, ✎ quill mark on scribe-authored nodes, radius grows with degree, archived hidden unless the chip asks.
  - **Interactions**: click → inspector (title/type/status/rev/author/source/tags/body + halo) with archive / promote / dismiss / **link mode** (click source, click target → `relates` edge); filter chips (type + proposed/archived + dynamic per-session chips); **proposed-review lane** with ✓/✕ (promote = node update to `status:active` — nothing auto-actives, M6 wires the scribe that feeds it); add-node modal (type/session/title/body/tags); **export modal with the §13.11 privacy confirm** (strip-authors checkbox + session filter) → browser download; import file-picker → POST → merge result toast; 5 s digest poll refetches the graph only on change.
  - **Browser-verified on the dev box** (preview tooling, real Chromium): pane renders the demo graph exactly per brand; node click → inspector + halo; review lane ✓ on a proposed node in demo mode degrades with the clear note "promote failed: demo mode: minti-cland not installed"; **zero console errors/warnings**; mobile-width (375 px) wraps header + chips with nothing broken (graph squeezes — desktop-first like the rest of the mock, noted).
  - **Honest scope**: the UI→live-daemon path reuses byte-identical CLI invocations already smoke-proven in M1–M4, but the full *UI-add → gossip → other member's CLI* loop on a real Clan is deferred to **M6's end-to-end VM verification** (the plan's final gate covers exactly that: the graph converging in *each* member's workspace tab). `go vet` + `go build` clean on the workspace module.

- **Clan Memory M4 done** (2026-06-11, this session): the **Clan Blueprint** (spec §13.10) — the portable single-file snapshot a fresh Clan inherits at `create`. ~900 LOC incl. tests.
  - **First real T3 *code* delegation, honestly accounted**: `blueprint.go` + `blueprint_test.go` (~440 lines) were generated by qwen3.6 via the new reusable `scripts/t3-codegen.py` driver (~45 s of local 5090 compute, two tight specs). **T2 review caught 4 implementation bugs** — 2 compile (`sha256.Sum256` array/slice misuse; `bp` referenced inside its own composite literal) and **2 logic** (session-filter edge direction BACKWARDS — contributions point node→session, it kept targets of edges *from* the session; member-type node ids omitted from the pseudonym set, which would have erased non-author member-node ids to "") — **plus 3 test-file defects** (a `*Graph` treated as a Blueprint, a self-contradictory strip-authors assertion block, unused imports) and 1 fixture artifact (the `node()` helper embeds the id in the Title, tripping the no-leak assertion — titles are free text, strip-authors pseudonymizes identifiers per §13.11). Verdict for the delegation log: viable for pure single-file boilerplate, but the review pass is **load-bearing** — both logic bugs would have shipped silent data corruption/privacy leaks.
  - **`blueprint.go`** (pure, stdlib-only): `ExportBlueprint` (deep copy via Merge, session-subgraph filter, sorted-identity pseudonymizer over authors + edge creators + member-node ids with edge-endpoint remapping, stats, `source_clan` pre-hashed); `BlueprintChecksum` (checksum-field-emptied compact-JSON sha256 — the toolexec canonical-bytes idiom, byte-stability pinned by a conformance test); `ValidateBlueprint` (kind / version ≤ supported / version ≥ 1 / graph present / checksum, distinct errors); `MarkImported` (source→"import" preserving rev/updated_at so re-imports stay idempotent under LWW merge).
  - **Service + endpoint**: `Service.ImportGraph(g, mode, from)` — merge = §13.4 union audited as `memory.import`, replace delegates to `Replace`; `POST /clan/memory/import` validates **server-side** (a client that skips its own validation still can't land a tampered file) and enforces the M0-review F4 gate: **`mode:"replace"` requires the TCP peer to be this machine** (loopback or any local interface IP — the CLI dials the configured listen address, which may be a LAN IP, so pure-loopback would have broken legitimate local use). 3 handler tests pin the gate (remote replace 403 / remote merge 200 / loopback replace 200) + tampered-import 400.
  - **CLI**: `memory export [--out f] [--session id] [--strip-authors]` (prints the §13.11 privacy warning — no silent export; suggests the create command), `memory import f [--replace]` (client-side validate for fast clear errors), `create --from-blueprint f` (validates BEFORE founding so a corrupt file can't leave a half-created Clan; seeds memory.json directly).
  - **`scripts/memory-m4-smoke.ps1` 6/6 PASS**: source Clan (session + finding + decision + system event) → export + `--strip-authors` variant (founder's member id absent, `member-N` pseudonyms present) → corrupt ONE byte → import AND create both reject with the checksum error, no half-created Clan → `create --from-blueprint` inherits 3 nodes all `source=import` with rev preserved → a joiner gossips the inherited graph (converged in 6 s). One smoke-script fix: PS 5.1 turns native stderr into terminating ErrorRecords under EAP=Stop + `2>&1` (the documented dev-environment quirk) — EAP scoped around the two deliberately-failing calls.
  - **Full `go test ./...` green** (19 packages; memory suite now 33 tests).

- **Clan Memory M3 done** (2026-06-11, this session): the **Scribe role** (spec §13.8) — the Orchestrator's inverse: the strongest node thinks, the weakest capable node remembers. ~600 LOC incl. tests.
  - **Capability + pins on the wire**: `Advertisement` gains `scribe_capable` (healthy local runtime + ≥1 RESIDENT model — remote APIs deliberately excluded; ambient distillation must be local) + `pinned_scribe`. **Adjacent latent gap fixed while there**: `buildPayload` never put `PinnedOrchestrator` on the wire — peers' step-down logic reads `LatestAd.PinnedOrchestrator`, which was always false; only self-election ever saw a pin. New `advertise.Service.Pins` closure now advertises both pins from clan state.
  - **Selection (`election.selectScribe`)**: INVERSE of `selectCandidate` — lowest `reasoning_score` among scribe-capable members, ties → oldest `AdmittedAt` → lowest `member_id`; `pinned_scribe` restricts the set (multi-pin → lowest id, §5.6 mirror); liveness gates mirror selectCandidate exactly. Orchestrator recomputes **every heartbeat tick** (cheap sort), persists `Clan.CurrentScribe` on change (R2 discipline), and emits the winner in the heartbeat `scribe` field; followers adopt from accepted heartbeats (incl. adopting "" so a withdrawn scribe clears everywhere), persist-on-change, seed from disk at startup (no restart flap). No lease — memory loss is non-fatal by design.
  - **Second latent gap fixed — follower delivery-liveness**: a follower never *sends* heartbeats, so the Orchestrator's only liveness signal for it was the **90 s ad-freshness window** — a dead Scribe would stay selected for up to 90 s (the first smoke run caught it: re-selection never happened inside a 30 s poll). Now every successfully *delivered* heartbeat (any HTTP status — a 409 peer is alive, just disagreeing) `TouchLive`s the peer on the Orchestrator, so "re-select on heartbeat-miss" works on the 2 s cadence. Re-selection observed in 6 s on the rig.
  - **Surfaces**: `GET /clan/scribe` + `POST /clan/pin-scribe` (mirrors `/clan/pin`, Bump-propagated) — both added to spec §10; CLI `minti-cland scribe [--json]` + `pin-scribe --self|--clear`; `Clan.CurrentScribe`/`Clan.PinnedScribe` persisted. New smoke hatch `MINTI_CLAND_FORCE_SCORE=<n>` (implies FORCE_HEALTHY incl. the R1 health gate — the first smoke run also caught that gap) so a localhost rig gets deterministic node strengths.
  - **10 unit tests**: lowest-score wins, capability gate, none-capable → "", 1-node scribe==orchestrator==self, pin override, multi-pin lowest-id, equal-score oldest-join tiebreak, refresh persists-on-change, dead-scribe re-selection, follower adopt + clear + persist, heartbeat carries the field.
  - **`scripts/memory-m3-smoke.ps1` 6/6 PASS** (3 daemons, forced scores 70/50/30): A orchestrates, C (30) scribes, all three `scribe --json` agree; kill C → **re-selected to B in 6 s**; `pin-scribe --self` on A moves the scribe to A against the score order, `--clear` restores B.
  - **Regressions clean**: Phase E smoke 9/9 + Memory M2 smoke 9/9 re-run after the engine changes (delivery-liveness + scribe emission); full `go test ./...` green across all 19 packages.

- **Clan Memory M2 done** (2026-06-11, this session): the memory graph now gossips — spec §13.5 wired into the election heartbeat as the third digest passenger, plus §13.7.1 system auto-events. ~700 LOC incl. tests.
  - **One real protocol gap found + spec-amended during implementation (P3: spec first, then code):** heartbeats flow **one way** (Orchestrator → peers), so a digest only in the *request* would mean a **follower's edits never propagate** — fatal for distributed research where every member contributes (the H-2 revocations passenger quietly has the same asymmetry; tolerable there, not here). Fix: the heartbeat **response** (`election.HeartbeatAck`) now carries the receiver's own `memory_digest`; the Orchestrator compares each ack and runs the same fetch-and-merge against that follower. Follower edits reach the Orchestrator in ≤1 round, everyone else on the next round's request leg (~4 s total). §5.3 passenger table + §13.5 updated with the response-leg paragraph and the honest convergence bounds (1 round orchestrator-edits, 2 rounds follower-edits).
  - **`election/`**: `Heartbeat.MemoryDigest` (omitempty, mixed-version safe) emitted at both build sites via `EngineOpts.MemoryDigest` (cached closure — never re-hashes on the 2 s path); `handleAckResponse` decodes 200 acks (4 KiB-bounded) and fires `OnHeartbeatAck` in its own goroutine (best-effort, never disturbs cadence); `Handlers` gains the optional `MemorySync` third passenger + `MemoryDigest` for the ack; `OnElectionWon(term, reason)` hook (engine stays policy-free — main.go filters bootstrap).
  - **`memory/sync.go`**: `Syncer.MaybeSync` — near-verbatim port of `revocations.Syncer`: cached-digest compare (no I/O), per-peer in-flight dedup, HMAC `GET /clan/memory`, **4 MiB fetch guard** (spec §13.5 anti-wedge headroom over the 2 MiB write cap), `ApplyRemote` persist-only-on-change (audited by the service).
  - **System auto-events (§13.7.1)**: `Service.RecordSystemEvent` (deterministic id → every observer mints the same node, union dedups); `membership.Service.OnEvent` hook fired on RedeemInvite/Welcome/knock-accept (`member_joined`) + Revoke (`member_revoked`); election failovers (`reason != bootstrap`) via `OnElectionWon`. **Honest gap:** no `member_left` event in v1 — a §3.6 self-leave wipes the leaver's local state before anything could gossip, and no survivor observes it (there is no leave broadcast in v1); documented in the membership hook comment.
  - **7 syncer unit tests** (empty-digest skip = pre-§13 compat, matching-digest no-op, mismatch→fetch+merge+idempotent-repeat, fetch-error preserves local, unknown-addr defers, oversize-response dropped, in-flight dedup holds concurrent fetches to 1) + **2 engine ack tests** (both legs: emitted body carries our digest, ack callback fires with theirs; nil-callback never decodes) + membership/election suites green.
  - **`scripts/memory-smoke.ps1` 9/9 PASS** (two daemons, 127.0.0.1:17984/5): `member_joined` system node gossips at join; orchestrator-side research session + finding reach the follower in one round (request leg); **follower-side fact + archive reach the Orchestrator (response leg — the new assertion)**; kill the Orchestrator → survivor writes the `election failover: term 2` node; restarted daemon converges to the full union (digest-equality poll, settled at 20 s: restart 2 s + R6 startup grace 6 s + lease-silent election + survivor's same-term-preferred accept + first ack-pull). First run failed here with a fixed 10 s sleep — root-caused to the legal heal sequence above (`ApplyHeartbeat` accepts a same-term heartbeat from the locally-preferred candidate, then the ack leg pulls the survivor's superset), not a protocol bug; the poll is the honest fix.
  - **Phase E smoke regression-clean** (9/9 steps, election/failover/term-bump/restart-accept all unchanged). The production NSSM `minti-cland` service was untouched throughout (the smoke's name-based kill can't reach a LocalSystem process from a non-elevated shell — PID 8760 stable across the run).

- **Clan Memory M1 done** (2026-06-11, this session): the cland memory core — graph store + service + HMAC endpoints + CLI, implementing spec §13.1–§13.7 (gossip itself is M2). ~1,500 LOC incl. tests across `cland/internal/memory/{graph,service,handlers}.go`, `state.go` additions, and `cmd/minti-cland/memory_cmds.go`.
  - **`graph.go`** (pure layer — no I/O/locks/clock): normative §13.1 types with declaration-order json tags; caps consts; `CanonicalNodeBytes` (the §13.4 LWW hash input); `Merge` (LWW winner `(updated_at, rev, sha256)` + edge set-union local-wins + sorted output so equal sets are byte-equal); `Digest` (sorted `n|id|rev|RFC3339Nano` + `e|from|to|rel` lines, archived included, empty = sha256("")); `NewUUIDv4` + `DeterministicEventID` (sha256-fold, P1 hand-rolled); write-path validators (merge stays permissive per §13.4).
  - **`service.go`**: mutex service owning the working copy + **cached digest recomputed under the same lock as every mutation** (M0 carry-note); §13.4 **origin-monotone HLC-lite stamp** `max(now, graph_max+1ns)` on the write path; daemon-set provenance (client-supplied author ignored; provenance immutable on update, rev bumps from the STORED node so stale snapshots can't rewind); caps → wrapped `ErrCap`; `ApplyRemote` (permissive merge, persist-only-on-change) + loopback-only `Replace`; audit events `memory.{node,edge,sync,import}`.
  - **`handlers.go`**: `GET /clan/memory`, `GET /clan/memory/digest` (**spec amendment**: digest endpoint added to §10/§13.6 — the workspace's 5 s change-poll must not transfer the whole graph; gossip never uses it), `POST /clan/memory/{node,edge}`; `ErrCap`→409, validation→400; origin extractor injectable for tests (cleaner than peers' parallel-context-key hack).
  - **`state.go`**: `LoadMemory/SaveMemory` via `any` (avoids the state↔memory import cycle the typed Revocations pattern would hit); mode **0600** (distillates may carry chat content — clan.json treatment, not revocations' 0644).
  - **CLI** (`memory_cmds.go`): `memory list|show|add|link|archive|digest` + `memory research start|close|list`, all `--json`, id-prefix resolution, `add --session` auto-mints the §13.7 `contributes_to` edge.
  - **20 unit tests green**: REQUIRED byte-stability conformance test for canonical bytes; digest permutation-invariance/sensitivity/empty-constant; merge commutative+associative+idempotent over 25 seeded conflicting graph pairs; LWW (newer-ts, equal-ts→rev, equal-both→hash both-directions-converge); tombstone propagation + documented resurrection; edge dedup local-wins; caps (count + size + per-field; merge-exempt fill then write-409; update-at-cap still allowed); REQUIRED origin-monotone test (poisoned ~2036 timestamp → honest edit still wins through re-merge); provenance daemon-set + immutable; persistence roundtrip incl. digest cache rebuild; two-service cross-sync convergence + idempotent re-apply; replace.
  - **`scripts/memory-m1-smoke.ps1` 8/8 PASS** against a real daemon (ephemeral Clan on 127.0.0.1:17992): empty-graph digest equals the spec constant; research start + add --session; `list --json` provenance == daemon identity; contributes_to edge present with daemon-set created_by; archive bumps rev + moves digest; **daemon restart → graph + digest + session survive** (memory.json); cleanup kills the daemon.
  - **One real bug caught by the smoke** (worth remembering): Go's `flag` package stops parsing at the first positional, so `research start "title" --config X --state Y` silently swallowed the flags and the CLI **talked to the production :7777 service** (the pre-§13 M5-B binary → 404). Fixed with `splitPositionals` (leading positionals collected, trailing flags re-parsed) across all five positional subcommands; the committed smoke deliberately keeps the flags-after-positional order as the regression test.
  - **Deferred to M2** (per plan): heartbeat `memory_digest` passenger + `MemorySync` third passenger + `memory/sync.go` + system auto-events + two-daemon convergence smoke.

- **Clan Memory M0 done** (2026-06-11, this session): protocol spec + 3-LLM peer review for the new Clan Memory subsystem — a gossiped, Clan-owned, Karpathy-style curated memory **graph** for distributed research, with an elected **Scribe** role on the *weakest* capable node (inverse of Orchestrator selection), explicit research sessions, a live force-graph view in the Clan Workspace (M5), and a portable single-file **Clan Blueprint** importable at `create`. Approved plan at `~/.claude/plans/we-can-still-do-transient-glade.md` (M0..M6, ~4,450 LOC). **Code does not exist yet — this is Phase M0 (spec + review only).** Memory M1 (cland graph store + endpoints + CLI) gated on this commit.
  - **`docs/clan-protocol.md` v0.3 → v0.4** — new top-level §13 *Clan Memory* (~200 lines): data model (8 node types, 4 statuses, 6 edge relations; caps 2,000 nodes / 8,000 edges / 2 MiB / 8 KiB body) ; `memory.json` 0600 storage; deterministic ids for system events (sha256-fold to UUID shape); CRDT-lite merge — LWW winner `(updated_at, rev, sha256(canonical_bytes))`, tombstones as ordinary LWW edits, edge set-union, dangling-edge tolerance, permissive-merge/strict-write split; content-versioned digest (sorted `n|id|rev|ts` + `e|from|to|rel` lines — deliberately UNLIKE §3.5's set-only revocations digest, because LWW edits must move it) cached + injected into the heartbeat as the **third digest passenger**; 4 MiB fetch guard over the 2 MiB write cap (anti-wedge headroom); peer-equal writes with daemon-set provenance; research sessions as plain nodes + `contributes_to` edges; **Scribe role** = lowest `reasoning_score` among `scribe_capable` actives (oldest-join then lowest-id tiebreaks, `pinned_scribe` override, Orchestrator-authoritative via heartbeat `scribe` field, NO lease — re-select on miss); scribe distillation duty (small local model, ≤5 strict-JSON proposals/pass, 120 s debounce, tolerant parser, everything lands `status:"proposed"` behind a human promote gate); Clan Blueprint (canonical checksum, `source_clan` pre-hashed, `--strip-authors` pseudonymizer, import-at-create + merge-import, `--replace` loopback-only); privacy + v1-honesty section. Also: §5.3 gains the heartbeat-passengers table (retroactively documenting H-2/H-3 digests), §10 gains the 4 memory endpoints + the missing H-2 `GET /clan/revocations` row, §12 gains OQ-8 (delta sync) / OQ-9 (compaction) / OQ-10 (blueprint signing). Explicit in §13: **unlike the audit log §9.1, Clan Memory IS gossiped.**
  - **`scripts/memory-peer-review.py`** — near-verbatim copy of the m8 driver (per `reference_local_llms.md` the copy-edit was cheaper as T2 than a T3 round-trip; the reviews themselves are the T3 value). 58 KB prompt × 3 models: qwen3.6 (think:false, 33 s), deepseek-r1:32b (54 s), gemma4:31b (87 s). Artifacts at `scripts/m4-reviews/memory-phase0-{qwen3.6_latest,deepseek-r1_32b,gemma4_31b}.md` + `memory-phase0-input.txt`.
  - **Verdicts**: qwen3.6 "ship after 1..5"; deepseek-r1 "ship after 2,3,5"; gemma4 "ship after 6,9". Triage at `scripts/m4-reviews/memory-phase0-triage.md`: **6 findings folded** — F1 clock-skew (gemma4 demolished both qwen's receiver-clamp [breaks convergence] and the plan's "rev helps" wording [rev fallacy]; folded fix = **origin-monotone HLC-lite stamping** `max(now, graph_max+1ns)` at the write path — poisoned clocks can't lock anyone out, convergence untouched); F2 scribe pending-proposal budget (≤200 un-reviewed; consensus finding — a runaway scribe could grind the 2,000-node cap); F3 canonical-bytes made normative (declaration-ordered structs + explicit json tags + REQUIRED byte-stability conformance test); F4 `mode:"replace"` import restricted to loopback (gemma4: destructive remote primitive behind a *shared* HMAC key = §7.1 insider surface; also documented that one-member replace doesn't make the Clan forget — gossip merges it back); F5 over-cap honesty (merge-exempt caps → read-only graph past cap, no v1 compaction, recovery = blueprint reset; OQ-9); F6 heartbeat passenger cost quantified (~192 B / 3 digests). **4 overruled with reasons**: deepseek's digest "order swap false positives" (backwards — sorting exists precisely so order is irrelevant), deepseek's "skew → divergent winners" (tuples travel with the node; gemma4: "convergence is guaranteed"), qwen's "allow remote import" (merge-gossip already covers it with less surface), deepseek's "sorted strip-authors leaks" (random-UUID order carries nothing; gemma4's cardinality-leak note accepted as inherent v1).
  - **All three reviewers explicitly approved**: the LWW tuple total ordering, tombstone-as-edit (+resurrection documented), edge set-union, permissive-merge/strict-write split ("the only way to prevent sync-wedging" — gemma4), the cached-digest closure mandate, all four scribe edge cases as specced (incl. 1-node scribe==orchestrator and leaseless flapping), the distillation human-gate, the blueprint checksum trick, and the scribe-on-weakest-node inversion itself ("elegant", "novel and effective").
  - **Carried into M1 as implementation notes**: digest-cache recompute under the service mutex on merge; canonical-bytes conformance test REQUIRED; dedicated unit test for the origin-monotone stamp (poisoned-future-timestamp recovery); M3's test list already covers deepseek's scribe-edge-case ask.

- **M8 Phase 0 done** (2026-06-06, this session): protocol spec + 3-LLM peer review for the new knock-by-clan-id Clan-joining flow. The user's earlier "wouldn't it be nicer if the joiner only needed the Clan ID and the existing operator approved in the TUI?" question (mid M7.6) drove this — a third path alongside §3.2 invite-token and §3.3 BIP39 paste-key. Live in-person / Signal-call onboarding pattern (Signal-pairing-light). Trust bootstrap: ephemeral X25519 ECDH between joiner and receiver + a Short Authentication String the operator and joiner visually verify with each other over a side channel. **Code does not exist yet — this is Phase 0 (spec + review only).** Phase 1 (~810 LOC of cland Go) gated on this commit.
  - **`docs/clan-protocol.md` v0.2 → v0.3** — new §3.4 "Knock flow (live in-person / Signal-call onboarding)" (~250 lines); renumbered existing §3.4 Revocation → §3.5, §3.5 Self-leave → §3.6. Spec includes: wire shapes (POST /clan/{knock, knock-list, knock-accept, knock-deny, knock-deliver}); cryptographic core (X25519 ECDH → HKDF-SHA256 with `clan_id ‖ knock_id ‖ joiner_x25519_pub ‖ receiver_x25519_pub` binding → 32B AES-256 key + 4B SAS source + 12B GCM nonce; ciphertext sealed AES-256-GCM with `aad=knock_id`); 30-bit SAS rendered "XXXX-XXXXX" (bumped from initial 20-bit after peer-review surfaced the pubkey-grinding attack); mutual SAS confirmation (CRITICAL — joiner-side `y/n` block before any state apply); joiner-side IP-allowlist on /clan/knock-deliver; full state machine; threat model with 7 attacker capabilities + defences; TTL bounds (60s..15min, default 5min); identity persistence + cert_priv trust transfer.
  - **`scripts/m8-peer-review.py`** — Python driver, mirror of `scripts/m4-phaseE-review.py`. Sends 4 documents to each of `qwen3.6:latest` (think:false), `deepseek-r1:32b`, `gemma4:31b`: existing §3.1-§3.3 membership context, the new §3.4 being reviewed, existing cland code shapes the plan reuses, the M8 implementation plan. Reviewers ran in 35-81s each (~40 KB prompt × 3 = ~$0 of compute on the local 5090). Outputs at `scripts/m4-reviews/m8-phase0-{model}.md`; input prompt cached at `m8-phase0-input.txt`.
  - **Reviewer verdicts**: qwen3.6 "ship after fixing 3, 7"; deepseek-r1 "ship after fixing 4, 5, 6"; gemma4 "ship after fixing 4, 5". Triage at `scripts/m4-reviews/m8-phase0-triage.md` enumerates the 4 findings folded into the spec + the 1 finding overruled with documented reason.
  - **Findings folded (consensus + high-severity)**:
    - **F1 — 20-bit SAS too weak** (deepseek + gemma4): bumped to 30 bits. gemma4 articulated the concrete attack: a MITM who intercepts `knock_id` can generate ephemeral X25519 keypairs offline and HKDF-grind until finding a pubkey whose SAS collides with the operator's displayed value. 2²⁰ HKDF iterations = milliseconds on modern hardware. 2³⁰ = minutes-to-hours, beyond the 5-min TTL.
    - **F2 — IP allowlist on /clan/knock-deliver** (deepseek + gemma4): joiner records the `receiver_addr` it knocked, refuses delivery POSTs from any other source IP. Shrinks the joiner-side attack surface from "anyone with the knock_id" to "only the IP we contacted".
    - **F3 — Mutual SAS confirmation** (qwen3.6 only — CRITICAL): joiner-side CLI BLOCKS for explicit `y/n` confirmation BEFORE arming `/clan/knock-deliver` acceptor. Without this, a MITM substitutes pubkeys, derives an attacker-controlled key, delivers an attacker-controlled blob, joiner blindly opens it (because GCM verifies with that key) and joins the attacker's "Clan" — the receiver-operator's SAS check is irrelevant because no traffic reaches the receiver. Treated as a blocker despite single-reviewer flag because it's load-bearing for the entire protocol's MITM defence.
    - **F4 — Document `clan_cert_priv` trust transfer** (qwen3.6, doc-only): new paragraph noting that accepting a knock grants the joiner the Clan's TLS private key (per §10/R1 unitary-trust model — same as §3.2/§3.3, but worth making explicit in the new flow's docs).
  - **Finding overruled with reason**: deepseek's "receiver-side state consistency" concern — gemma4 explicitly considered the same scenario and dismissed it as "UX inconsistency, not protocol failure". CAS on the joiner guarantees a single-valued outcome; both receivers' audit logs faithfully record what they POSTed; the joiner's final state is the truth-of-record. Documented in `scripts/m4-reviews/m8-phase0-triage.md`.
  - **All three reviewers explicitly approved** (no changes): HKDF construction with `clan_id`+`knock_id` binding, deterministic nonce safety (per-knock unique key makes nonce-reuse safe), AES-GCM tag as receiver-authenticity proof, anonymous /clan/knock mirroring §3.2 /clan/join precedent, cross-Clan replay defence, per-knock_id CAS, 5s TUI poll cadence.
  - **Phase 0 commit gates Phase 1**. Phase 1 (~810 LOC across `cland/internal/membership/{knock.go, knock_handlers.go, knock_test.go}`, `cland/cmd/minti-cland/main.go` additions, and `cland/internal/membership/service.go` helpers) implements the v0.3 spec. The mutual-SAS-confirmation (F3) is the easiest place for an implementation shortcut to silently break the protocol — explicit test required.

- **M7.6 done** (2026-06-06, this session): first user-initiated mutation in the otherwise read-only `minti-status`. User asked "wouldn't it be nice to copy a command that joins another node to this Clan?" — surfaced by pressing `i`. Renders a new section below the Clan panel with the copy-pasteable `minti-cland join --token X --address Y --pin Z` line. The mint shells `minti-cland invite --ttl 5m --json` (existing CLI surface from Phase C-rest); auto-dismisses on TTL expiry via a tea.Tick scheduled for `time.Until(expires)`. Re-pressing `i` replaces with a fresh token.
  - **Why this is "first mutation"**: the M7 plan said v1 was read-only — no pin/rotate/peer-add/leave. Invite-minting writes to cland's InviteStore (Phase C-rest) but the cost of a leftover invite is bounded by its 5-minute TTL, so this is roughly as risky as running `minti-cland invite` from a regular shell yourself. Stays well clear of the "destructive op" line that pin/rotate-key/leave/revoke crossed.
  - **`internal/probes/clan/invite.go`** — `MintInvite(ctx, ttl)` shells the CLI subcommand, parses InviteResponse (`token` / `clan_id` / `clan_cert_pin` / `lan_address` / `expires_at`), and pre-renders the `JoinCommand` so the panel doesn't have to know the argument layout. Same `ErrPermissionDenied` graceful-degrade path as the other clan probes (EACCES on `clan.json` → footer shows the error, panel stays empty).
  - **`internal/tui/msgs.go`** — `inviteMsg` (result of mint) + `inviteExpiredMsg` (fired by `cmdInviteExpiryWatch` when the TTL elapses). `cmdMintInvite(ttl)` runs the probe off the UI goroutine with a 2s timeout.
  - **`internal/tui/app.go`** — new `m.invite *clan.Invite` field; `i` key triggers `cmdMintInvite(inviteTTL)` (5 min default); `inviteMsg` lands the invite + schedules the expiry watcher; `inviteExpiredMsg` clears the field. `inviteTTL` const documented inline.
  - **`internal/tui/panels/invite.go`** — renders the section when `m.invite != nil && !Expired()`; returns `""` otherwise so `layout.go` naturally omits it (panel above/below stay flush, no empty box). The join command lands on its own line with no surrounding chrome on that line — triple-click in modern terminals (wt, iTerm2, kitty) selects it cleanly for copy.
  - **`internal/tui/layout.go`** — inserts the invite section between Clan and the Addons + Harness bottom-row when present, in both wide (≥100 cols) and narrow (<100) modes.
  - **`internal/tui/panels/header.go`** — keybinds row now: `q quit · r refresh · i invite · ?/h help` (with `i mint 5m invite` in the expanded `?`-toggled help variant).
  - **Goldens**: 20 scenarios now (was 18). New: `invite_active` (with deterministic 4m31s remaining), `invite_nil` and `invite_expired` (asserted via `got != ""` check, not golden files since the result is empty). Existing `footer_no_err` + `footer_with_err` + `header_basic` regenerated to pick up the new keybind row.
  - **Live smoke on the daily-driver Windows host**: launched M7.6 binary via `launch.ps1` in a fresh `wt` tab against the 3-node Phase J Clan; pressed `i`; the panel appeared with the join line + 4m59s countdown; the command was copy-pasteable. (Actually running the join on a clean shell to confirm round-trip is operator work — VM-B is already a member; M5-D / a clean WSL would be the right test bed.)
  - **VERSION** in top-level Makefile 0.3.0-M7.5 → 0.3.0-M7.6; `status/debian/changelog` gains the M7.6 entry.

  Carried into M8 candidates:
  - **M8: knock-by-clan-id flow** — proper inverse UX. Joiner only needs the Clan ID (which is essentially public); existing operator approves the join in the TUI. Requires new cland endpoint (`POST /clan/knock`), knock store with TTL, accept/deny CLI + endpoints, 6-digit confirmation code pattern (Signal-style) so operator can visually verify without needing a separate trust channel. **~half-day to a day** of work; deserves its own plan file. User asked for this; M7.6 ships the easy paste-helper first so both flows coexist.
  - Render the .gif from M7.3's .tape (still no vhs).
  - macOS RAM-used via vm_stat.
  - Other mutations (pin/rotate-key/peer-add/leave/revoke) — still on hold.
  - M5-D Phase 4 rerun.
  - tui_test.go for the Model.Update flow (would've caught the M7.5 flap — still uncovered).

- **M7.5 done** (2026-06-06, this session): tick-merge fix in `minti-status` Update handler + `status/launch.ps1` for one-click interactive launch on Windows. Surfaced live: when the user ran the patched binary in a wt tab on the daily-driver against the 3-node Phase J Clan (VM-A Orchestrator at .102 / VM-B at .101 / Windows joined at .1), the Members count visibly oscillated **1 → 3 → 1 → 3** every 2 seconds, and CPU + GPU rows flapped to `(n/a)` between 30s slow ticks. Both symptoms had the same root cause: the fast/medium-tick probes (`ProbeOrchestratorOnly` for clan, `ProbeCheap` for sysinfo) return a State / Info subset that carries only fast fields; the slow fields (Members + Candidates + RecentElections; CPU model + GPU) are Go zero-valued. The Update handler was wholesale-replacing `m.clan` and `m.sys` on every tick, so the slow fields would land on the slow tick and get wiped every 2-5s thereafter.
  - **Fix**: 3-line merge per handler in `status/internal/tui/app.go`. On `sysInfoMsg`: when `msg.Info.CPUModel == ""`, copy from prior `m.sys.CPUModel/CPUCores`; same for `GPU`. On `clanMsg`: when `msg.State.Members` is empty AND prior `m.clan.Members` is non-empty, preserve; same for Candidates + RecentElections. After the fix the panel holds Members (3) steady; CPU + GPU stay populated forever after the first slow probe lands.
  - **Why golden tests didn't catch it**: tests render single panel calls with deterministic Info / State inputs — they exercise the panels, not the Model.Update flow. The bug is in the tick→merge→render pipeline, not the per-panel formatter. M7.6 candidate: add a `tui_test.go` driving `Model.Update` with a sequence of synthetic messages and asserting m.clan / m.sys retention across them.
  - **`status/launch.ps1`** (added in M7.x's live demo run, now retroactively committed): single-script entry point that sets `PATH=C:\Program Files\MINTI\cland;%PATH%` so the Clan probe finds `minti-cland.exe`, then runs `minti-status.exe`. Designed to be invoked from a fresh `wt.exe new-tab`, with usage notes documenting the two wt argument-parsing landmines that motivated it: (1) `;` inside an unquoted `-Command` value gets re-interpreted by wt as a subcommand delimiter, splitting the command into garbage; (2) wt also re-splits args containing literal spaces (e.g. `--title "minti-status (patched)"` → wt sees `(patched)` as the next positional and tries to launch it as the command). Both bit during the live demo this session. Keep `--title` a single word.
  - **VERSION** in top-level Makefile bumped 0.3.0-M7.4 → 0.3.0-M7.5; `status/debian/changelog` gains the M7.5 entry.
  - **Verification**: `go test ./internal/tui/panels/` all 18 goldens still green (no panel-level changes). Cross-compile + .deb path unchanged. Visual confirmation in the live wt tab — Members held at 3 steady, CPU/GPU steady at their slow-tick values.
  - **Carried forward**: actually render the .gif from M7.3's `.tape` (vhs still not installed); macOS RAM-used via vm_stat; Mutations (pin/rotate/peer-add/leave); M5-D Phase 4 rerun; the Model.Update sequence-tests mentioned above (M7.6 candidate).

- **M7.4 done** (2026-06-06, this session): cross-platform sysinfo polish in `minti-status`. M7's day-1 stubs reported `amd64 (Windows)` for CPU model and had no Uptime row on any platform; this commit flesh-fills the data on Windows + macOS (via the platforms' native sysinfo APIs, no shell-outs except a single `sw_vers` on macOS) and adds Linux's missing `/proc/uptime` read.
  - **Windows** (`sysinfo_windows.go`): CPU model from `HKLM\HARDWARE\DESCRIPTION\System\CentralProcessor\0\ProcessorNameString` via `golang.org/x/sys/windows/registry` (same module already pulled in for GlobalMemoryStatusEx); OS version via `ntdll!RtlGetVersion` with marketing-name semantics (Win10 build ≥22000 → "Windows 11"); uptime via `kernel32!GetTickCount64`. All direct syscalls, zero shell-outs. Tested live on the daily-driver: now reports `Windows 11 (build 26200)` / `Uptime 1h 54m` / `AMD Ryzen 9 9950X3D 16-Core Processor 32t`.
  - **macOS** (`sysinfo_darwin.go`): CPU model from `sysctl machdep.cpu.brand_string` (works on Intel + Apple Silicon — AS returns "Apple M2 Pro" etc.); OS version from `sw_vers -productVersion` (one fork-exec per medium tick — acceptable per cland's existing `pmset` precedent); uptime decoded from `sysctl kern.boottime` (16-byte struct timeval, first uint64 = boot epoch). RAM-used deliberately left at 0 since macOS doesn't expose a useful "available memory" via sysctl — properly computing it needs `vm_stat` semantics (file cache + compressed + speculative). Deferred to M7.5+ when the panel cares; golden labelled accordingly.
  - **Linux** (`sysinfo_linux.go`): added `/proc/uptime` read (first float = seconds since boot, rounded to seconds for display). Live VM test renders `Uptime 1d 19h 55m`.
  - **`sysinfo.Info`** gains `Uptime time.Duration`. Panel adds an Uptime row between Kernel and CPU; renders as `XdYhZm` (or just `Xs` sub-minute, or `(n/a)` when zero — exercised by the new `system_no_uptime` golden).
  - **Golden tests refreshed**: 18 scenarios now (up from 15). Three new: `system_windows`, `system_darwin`, `system_no_uptime`. Existing `system_linux` updated to include the new row. Test inputs are deterministic structs so the goldens lock per-platform rendering without needing actual platform execution at test time.
  - **Cross-compile matrix** stays green for linux/amd64, windows/amd64, darwin/amd64, darwin/arm64. .deb path unchanged. VM smoke + host smoke both verified the new fields land cleanly.
  - **Static `status/docs/example.txt`** re-captured against the live Phase J Clan — now shows the Uptime row (`1d 19h 55m`).
  - **Versions**: top-level Makefile `VERSION` bumped 0.3.0-M7.1 → 0.3.0-M7.4 so `make status-linux` stamps the right `-X main.version`. status/debian/changelog gains the M7.4 entry.

  Honestly still carried forward:
  - macOS RAM "used" needs `vm_stat` shell-out to compute properly (Mach's accounting differs from Linux's MemAvailable).
  - Render the actual `.gif` from the M7.3 .tape (vhs still not installed).
  - Mutations (pin / rotate-key / peer-add / leave / revoke).
  - M5-D Phase 4 rerun.

- **M7.3 done** (2026-06-06, this session): docs round-out for `minti-status`. Ships the iteration-tooling deliverable from the M7 plan plus a project-root pointer so future readers can find the dashboard without spelunking commit history.
  - **`status/README.md`** — what it shows (with the live `--once` snippet rendered inline), quickstart (`--once` / `--no-color` / `--refresh`), keybinds table, per-panel reference (probe + cadence + what it surfaces), refresh-tickers model, build matrix, tests + `-update` flag, vhs-rendering recipe, v1 scope guard + explicit out-of-scope list, source-tree map.
  - **`status/docs/minti-status.tape`** — vhs source for the README .gif. Catppuccin Mocha theme, 1400×760, 13pt JetBrains Mono, 8s capture window (long enough for the fast ticker to fire three times so a reviewer of the .gif can see the refresh badge tick). The .gif itself is gitignored; only the .tape source ships in the tree.
  - **`status/docs/example.txt`** — committed static `--once` snapshot captured live from the `minti-dev` VM (3.1 KB). Ensures the inline README example stays a real artefact even when vhs isn't installed; CI-friendly. Captured against the same live Phase J Clan 5725d958-… with 4 members visible, self-orchestrator at term=3368, 4 mDNS-discovered candidates, deduped election history showing `(×32)`.
  - **Makefile `status-gif` target** — calls `vhs status/docs/minti-status.tape`, fails fast with an install hint if vhs isn't on PATH. NOT a CI dep; this is authoring tooling. Linux note: vhs isn't in apt yet, so the message points to the GitHub release.
  - **`.gitignore`** — `status/docs/*.gif` added so the rendered build artefact never accidentally lands in a commit.
  - **Project root `README.md`** — `status/` row added to the source-tree layout block + a short paragraph pointing readers at `status/README.md` for the dashboard. Same edit also tightens the layout block to reflect M6-content + M6.1 reality: `mcp-servers/` lists `wiki`, `pack-manager/` mentioned with its real role, `packs/` lists the new addon metapackages, `branding/` mentions `minti-fetch`.
  - **Verification**: cross-compile still green (no source-tree changes). The `make status-gif` target was wired but NOT actually run this session — vhs isn't installed on the daily-driver and the .gif is non-blocking (the static `example.txt` covers the inline-readme case). Recording the .gif is a 30-second job once vhs lands; the .tape is ready.

  Knowns carried forward:
  - vhs render itself — needs `apt install` (no Debian package yet, GitHub release tarball) + `ttyd` + `ffmpeg`. Deferred until the next session that touches docs.
  - Mutations (pin / rotate-key / peer-add / leave), Windows + macOS CPU + uptime, M5-D Phase 4 rerun — all still queued for M7.4+.

- **M7.2 done** (2026-06-06, this session): golden-file tests for `status/internal/tui/panels/`. 15 scenarios cover every panel × the meaningful states it can be in: Clan in 4 (unaffiliated, self-orch, peer-orch, EACCES) + history-dedupe; Runtime in 2 (healthy w/ VRAM, down); System linux; Addons empty + populated; Harness none + opencode-full; Header + Footer (with/without last-err). Each test renders the panel and diffs against `testdata/golden/<name>.txt`; `go test ./... -update` regenerates after a deliberate rendering change. ANSI is stripped before diffing — colours are styling, not structure, and lipgloss colour-output depends on TTY detection that's brittle for goldens.
  - **Determinism fixes** (the prereq): `panels.Clan` gained a `now time.Time` parameter (was using `time.Since(e.At)` inline, wall-clock dep). `selfDisplay` now reads a package-level `selfOS` var that defaults to `runtime.GOOS`; tests pin it to `"linux"` so the goldens match cross-build platform. Single callsite in `tui/layout.go` updated to pass `time.Now()`.
  - **Files**: `golden_test.go` (helper + `-update` flag + ANSI strip), `clan_test.go` (5 scenarios), `runtime_test.go` (2), `misc_test.go` (8). 15 golden .txt files in `testdata/golden/`.
  - **Smoke**: `cd status && go test ./internal/tui/panels/ -v` → 15/15 PASS on the Win11 host in 0.34s. Cross-compile to linux + windows + darwin remains green (no new platform-specific code).
  - **Honest knowns**: `go test` requires Go on the box running it; the `minti-dev` VM doesn't have golang-go installed (only dpkg-buildpackage), so `make test` only works on the host (or any other box with Go). The deb-build path in the VM is unaffected. teatest itself (the full bubbletea event-loop driver) was reconsidered: panels are essentially pure functions, so plain Go golden tests give 90% of the value at zero extra dep cost — teatest stays a candidate for M7.3 if/when we add interactive state (mutations).
  - **Carried forward**: vhs `.tape` for the README .gif (the visual asset half of the "iteration" decision in the M7 plan), long interactive run, mutations (pin/rotate/peer-add/leave), Windows + macOS CPU model + uptime, M5-D Phase 4 rerun.

- **M7.1 done** (2026-06-06, this session): Clan panel quality fix in `minti-status`. The M7 commit's smoke run surfaced three real schema mismatches the panel hid behind dashes + stale ○ bullets; this commit wires through the actual `minti-cland` JSON shapes (sampled live from the running daemon, not guessed). After-shot for the same Clan: each member's OS, state, reasoning/system scores, last-ad age, plus a synthesised "(self) ★" row at top, plus a Candidates section, plus a deduped election history line `term 3368 ← a9f3df01… reason=bootstrap 4s ago (×32)`.
  - **`probes/clan/clan.go`** — `peersResp` matches cland's real wire schema: `reasoning_score` + `system_score` live under `latest_ad`, `last_ad` is an RFC3339Nano timestamp (not seconds-ago), `hardware.{gpu,vram_gb,ram_gb}` + `os` surface per-member, `heartbeat_seen` + `ad_generation` track liveness. `parseShow()` flipped to case-insensitive prefix matching against cland's actual labels (`Member ID:`, `Role:`, `Cert pin:`, `LAN addr:` — all of which my M7 parser missed because they're title-case). `readHistory()` now dedupes by `(term, winner)` and keeps a `Repeated` count, so the 32 identical "term=3368 ← a9f3df01… bootstrap" entries the engine emits every 3s collapse into one row tagged ×32.
  - **`tui/panels/clan.go`** — new `memberDisplay` row type; `selfDisplay()` synthesises the local-node row from `State.SelfAddress` + `runtime.GOOS` + `State.Role` since cland's `peers --json` deliberately excludes self. Member column order locked: `member · os · state · reason · sys · ad`. Candidates list surfaces below members ("Candidates (4): 10.0.2.15:7777 (mdns)  …"). Header row uses `lipgloss.Width`-aware padding so the bullet glyph + ANSI escapes don't push columns out of alignment.
  - **Cross-platform regression**: linux/amd64 + windows/amd64 host builds verified post-change; the new `runtime.GOOS` lookup in `selfDisplay` is a stdlib constant so no new platform-specific code. No build-tag changes.
  - **Smoke** in the `minti-dev` VM (same Phase J Clan 5725d958-…): all 4 members now visible with correct OS + scores (101 linux 35/22, .1 windows 50/66 active, .1 windows admitted 0/61); Candidates surfaces 4 mDNS-discovered addresses; election history one row instead of 32. STATUS panel + footer + other panels unchanged + still render correctly.

  Honest knowns carried forward:
  - The two `192.168.56.1:7777` rows in the live smoke represent the foreground Phase J `minti-cland.exe` (a9f3df01… member_id) + the M5-B Windows Service (b2ecb5c4… member_id) — both bound to the same host-only IP. The duplicate-address render is *accurate*, not a bug. M5-D resume should clean that up by uninstalling the foreground process.
  - The "173h ago" / "205h ago" `last_ad` ages are real (VM-B + Windows host haven't advertised since ~one week ago). The 3-node Clan needs both VMs warmed + Windows Service joined for fresh ads — that's the M5-D Phase 4 rerun, still parked.
  - teatest goldens, vhs `.tape`, long interactive run, mutations (pin/rotate/peer-add/leave), Windows + macOS CPU model + uptime fleshing — all still M7.2 work.

- **M7 done** (2026-06-06, this session): new `minti-status` Go binary — live read-only terminal-UI dashboard for a MINTI node. Surfaces in one screen what was previously spread across `minti-fetch` (one-shot bash), `minti-cland show/orchestrator/peers/election-history` (CLI inspection), and ad-hoc curl against `/minti/capabilities` + Ollama `/api/ps`. Plan at `~/.claude/plans/we-need-to-plan-async-candle.md`.
  - **`status/` new top-level Go module** (`module github.com/minti/status`, go 1.25, CGO_ENABLED=0). Layout matches existing `cland/` + `pack-manager/` (cmd/minti-status/, internal/{tui,probes,version}/). Three probe packages: `probes/sysinfo` (linux + windows + darwin variants — Windows uses direct kernel32!GlobalMemoryStatusEx syscall to avoid the `golang.org/x/sys/windows` binding-version dance, Darwin uses sysctl per cland's existing precedent), `probes/runtime` (loopback HTTP to minti-runtime + Ollama `/api/ps`), `probes/clan` (shells out to `minti-cland show/orchestrator --json/peers --json/election-history --json`; EACCES → degrades to `(sudo for clan details)`), `probes/addons` (parses `/var/lib/minti/packs/*.installed`), `probes/harness` (parses opencode.json + optional ~/.claude/settings.json).
  - **bubbletea + lipgloss + bubbles** (charmbracelet, MIT). Three independent tickers (`tickFast` 2 s = runtime + Ollama `/api/ps` + cland orchestrator; `tickMed` 5 s = sysinfo cheap + addons + cland peers+members+history; `tickSlow` 30 s = nvidia-smi + CPU model + harness re-parse). Each probe runs off the UI goroutine; a probe failure never crashes the UI — surfaces in the footer's `last-err:` line + degrades the affected panel. Keybinds `q`/`Ctrl+C`/`Esc` quit, `r` force-refresh, `?/h` help.
  - **Layout: responsive grid.** ≥100 cols → 2-column rows (System+Runtime up top, Addons+Harness on the bottom, wide Clan row spanning the middle). <100 cols → all panels stacked. lipgloss.JoinHorizontal handles the column composition. Panel widths via the shared `halfWidth=50` / `fullWidth=100` constants in `panels/common.go`.
  - **Cross-platform from day 1**: builds clean for linux/amd64, windows/amd64, darwin/amd64, darwin/arm64 via the new Makefile targets `status / status-linux / status-windows / status-darwin-{amd64,arm64} / status-all-platforms`. Build-tagged `sysinfo_{linux,windows,darwin}.go` keeps `/proc` reads + `nvidia-smi` shell-outs Linux-only; Windows + macOS get reduced day-1 stubs (RAM via syscall on Windows, `hw.memsize` sysctl on Darwin) that surface the platform's core data — full parity in a follow-up.
  - **.deb packaging** mirrors M6.1's `pack-manager/debian/`: Architecture: amd64, Build-Depends: just debhelper-compat=13, ships pre-built binary at `/usr/bin/minti-status`, `Recommends: minti-cland`. `make status-deb` target stages the cross-compiled binary at `status/minti-status`, runs `dpkg-buildpackage`, drops the .deb into `dist/`, cleans up. Same chmod-debian-files dance for vboxsf safety.
  - **`--once` mode** for non-TTY contexts (CI, SSH pipe). Hits all probes once synchronously and prints the rendered View to stdout — same output as the interactive first frame. `--no-color` (also via `NO_COLOR=1` env) disables ANSI.
  - **End-to-end smoke** in the `minti-dev` VM (NAT port-forward `127.0.0.1:2222`, Clan 5725d958-… alive with VM-A self-orchestrating): cross-compile linux/amd64 on host → scp to /tmp/ → run `--once` → all six rows populate correctly (Linux Mint 22.3, Ryzen 9 9950X3D, runtime 0.1.0-M3 healthy via ollama backend, llama3.2:3b marked ★ as the resident-with-highest-reasoning, Clan ID + role + pin + Orchestrator + term=3368 + lease countdown + 3 peers + last-3-elections from the history ring, opencode wired to minti-runtime with all 5 MCP servers detected, claude/no-preset, Addons/empty since we purged in the M6.1 smoke). `make status-deb` builds clean in the VM → `dpkg -i` installs binary at `/usr/bin/minti-status` → version reports `0.1.0-M7` → `--once` works identically → `dpkg --purge` removes it cleanly. Host (Windows 11 + RTX 5090) also runs cleanly, GPU panel correctly surfaces "GeForce RTX 5090  31.8 GiB (1.8 used)" via nvidia-smi.
  - **Honestly not yet covered**: teatest goldens (the test framework's there in the plan but not implemented this session — golden-file regressions land in M7.1); vhs `.tape` for the README .gif (deferred); interactive run for long enough to actually see the tickers fire and the panels update (verified by code review + --once snapshots, not by sitting in tmux); Windows/macOS cosmetic CPU + uptime fleshing-out (day-1 stubs report `amd64 (Windows)` style placeholders for CPU model — adequate but not pretty). Mutations (pin/rotate/peer-add/leave) intentionally absent in v1 per the approved plan; M7.1 candidate.
  - **One quality observation from the live smoke**: `minti-cland peers --json` returned 3 members but with empty reason/sys/last-ad fields, so the table renders bullet=○ everywhere. Likely the cland `peers --json` output schema is narrower than this panel assumes (or VM-A is the only one with full advertisements landing). Worth a follow-up: either cland enriches `peers --json` or minti-status falls back to merging `/clan/peers` + per-member `/clan/orchestrator` data. Not blocking — the panel still shows the membership + roles + addresses, which is the primary signal.

- **M6.1 done** (2026-06-06, this session): `minti-pack-fetch` now ships as its own .deb. Restores a real `Depends: minti-pack-fetch (>= 0.1)` chain on all three addon packs — `dpkg -i ./minti-pack-hermes3_*.deb` without pack-fetch installed now fails cleanly with "dependency problems - leaving unconfigured" instead of silently breaking at postinst.
  - **`pack-manager/debian/`** scaffold: control (Architecture: amd64, Build-Depends just debhelper-compat=13), changelog (0.1.0-M6), copyright (MIT, matches recon), rules (skips dh_auto_build/test — binary is pre-built; matches how cland .zip/.tar.gz ship pre-built artifacts), install (binary lands at `/usr/bin/minti-pack-fetch`), source/format (3.0 native).
  - **Path decision**: deb installs at `/usr/bin/minti-pack-fetch`, install.sh's bare-binary deploy stays at `/usr/local/bin/minti-pack-fetch`. Both are in $PATH; PATH order puts /usr/local/bin first so an operator's manual deploy overrides the deb. Standard Debian convention (dh_usrlocal would reject the /usr/local path otherwise). Addon postinst scripts changed from `/usr/local/bin/minti-pack-fetch` → bare `minti-pack-fetch` (PATH lookup).
  - **Makefile** gained `pack-fetch-deb` target (mirrors the build-pack macro but adapted for the pack-manager source layout — stages the pre-built binary at `pack-manager/minti-pack-fetch` before `dpkg-buildpackage` and cleans it up after). `build-pack` macro extended to `chmod -x` `debian/source/format` (vboxsf workaround: this file would otherwise inherit the executable bit and break `dpkg-source --before-build`).
  - **install.sh** gained a pack-fetch deployment block between cland and opencode. Searches the same three locations as the other binaries (native build, dist/-linux-amd64, dist/-bare); installs to `/usr/local/bin/minti-pack-fetch` mode 0755; creates `/var/lib/minti/packs/` as a side effect. Surfaced in the final summary block as `Pack-fetch:` and the "addon packs" next-step block already documents `MINTI_PACK_NO_FETCH=1`.
  - **Manifest parser bug surfaced by smoke**: `minti-pack-fetch` was using `json.Decoder.DisallowUnknownFields()`, which rejected the `_comment` field I added to `wiki-simple/manifest.json`. Switched to standard `json.Unmarshal`. The `_comment` idiom is broadly used in JSON config files and future schema additions shouldn't break older fetchers. Two-line code change in `pack-manager/cmd/minti-pack-fetch/main.go`.
  - **Kiwix ZIM pinned**: `packs/wiki-simple/manifest.json` updated from a 2024-06 placeholder + empty sha256 to `wikipedia_en_simple_all_nopic_2026-05.zim` (937 MB, ~250k articles, no images) with SHA-256 `503b74027d101ec13b272f1ebbd23473e1d260be71a35a71141dfc985fd20d0a` — cross-checked against the published .sha256 sidecar and the 302-redirect Digest header from the kiwix.org load balancer. The fetcher's strict SHA-256 verify path is now exercised whenever someone actually downloads.
  - **End-to-end smoke** in the `minti-dev` VM (NAT port-forward `127.0.0.1:2222`): clean state → `dpkg -i hermes3.deb` alone fails with unmet `minti-pack-fetch` dep (correct); install `pack-fetch.deb` → `/usr/bin/minti-pack-fetch` lands + reports version 0.1.0-M6; reinstall hermes3 with `MINTI_PACK_NO_FETCH=1` → postinst runs through, marker written, `minti-fetch` shows `Addons: hermes3`; `apt install ./mistral.deb ./wiki-simple.deb` resolves the full dep transitive (pulled kiwix-tools, aria2, libkiwix12t64, etc.) → both extra markers land → `Addons: hermes3 mistral wiki-simple`; purge addons → markers vanish, pack-fetch survives (correct Depends-not-Pre-Depends semantics); purge pack-fetch → /usr/bin/minti-pack-fetch gone. Both `/usr/local/bin/` (install.sh) and `/usr/bin/` (deb) pack-fetch coexisted cleanly during the test; PATH put /usr/local/bin first.
  - **Known minor cosmetic from the smoke**: dpkg still warns "Configuration file /usr/lib/systemd/system/minti-kiwix-serve.service is marked executable. Proceeding anyway." Same root cause as the other vboxsf Phase E findings — the source tree on the shared folder has the file marked executable; the build-pack copy stage chmods debian/ files but doesn't touch `systemd/*.service`. Fix is a one-line tweak to the build-pack macro; deferred since the warning is purely cosmetic (the unit symlinked + started fine).

### Next after M5
- **M5.1 (lightweight follow-on)**: tray app + Fyne GUI; `runtime.force_healthy: true` config knob (currently env-var only); cold-boot test via Task-Scheduler-on-boot orchestration; 24h Defender soak script; **live macOS install** when a Mac is booted.
- **Phase J Pass 4 / M5-D live**: full 16-test PowerShell acceptance gate against the service-managed Windows + both VMs. **M5-D Phase 4 rerun was paused** mid-session pending VM bring-up; `scripts/m5-phaseD-validate.ps1` has the JSON-schema fixes (tests 2 + 5 read `current_orchestrator` / `current_term` / `latest_ad.os` / `last_ad` + 90s window instead of the legacy fields the original draft assumed). Resume: bring up both VMs, run the script from elevated PS.
- **M6 (broader)**: package `minti-pack-fetch` as its own .deb; install.sh deploys it; signed repo; remaining tool packs (webapp, wireless, forensics); code-signing for Windows MSI + Apple notarised .pkg.
- **M4.1**: remote-API backend → Cardputer-as-Orchestrator demo.
- **M3.5 (squeeze-in candidate)**: tool-use content blocks in `/v1/messages` (Claude Code's native tool-call shape over the API rather than via separate MCP stdio). Currently rejected with 400.

### Future 🔲
- M6: Security hardening + remaining packs (webapp, wireless, forensics) + Pack Manager + **code-signing for Windows + Apple Developer notarisation** (unlocks signed .msi + notarised .pkg, removes Defender Folder-Exclusion + Gatekeeper xattr-strip workarounds).
- M7: Old-hardware smoke test (real 10-yr-old Mac + 4-6 GB VRAM "secondary box" joining the Clan).
- M8: v1.0 release.

### PRD §6.5 deviations (M5)
- **No signed `.msi`** — needs an Authenticode cert. Ship `.zip + .ps1` instead. M6 brings a signed MSI via WiX or NSIS.
- **No signed `.pkg`** — needs an Apple Developer account + `xcrun notarytool`. Ship `.tar.gz + .sh` instead. M6 brings a notarised + stapled `.pkg`.
- **No `windows.SetFileSecurity` Go-side ACL hardening on identity.json** — installer's `icacls /inheritance:r` provides the equivalent guarantee with much smaller surface. The installer becomes load-bearing for Windows ACL security; documented in code + READMEs.
- **macOS battery probe uses `pmset -g batt` shell-out (not IOKit)** — keeps cland's zero-cgo build property; 30s probe cadence makes the fork-exec cost trivial.

---

## How to resume work

### Single prompt to start the new chat

```
M4 + M5-A/B/C all DONE (2026-05-28). cland Phases through M4-J shipped;
M5-A foundations + M5-B Windows NSSM service (live install verified) +
M5-C macOS launchd daemon (build clean, live test deferred — no Mac)
committed in 25e1cf1, 4e5373b, dbd6e10. M5-D `scripts/m5-phaseD-validate.ps1`
written but live run is operator-session work (needs both VMs warmed,
Windows Service join-ed to the Clan, elevated PS for DACL audit).
Read memory + STATUS.md for the full chronicle.

Operator follow-ups captured for the next session:
  - **Windows Service is currently installed + Running on the host**
    at version 0.2.0-M5 in pre-Clan idle (member_id b2ecb5c4-...).
    Uninstall (preserve state):
      powershell -ExecutionPolicy Bypass -File C:\Temp\m5b-cland-install\uninstall-cland.ps1
    Or have it join the Phase J Clan to make M5-D's election test work:
      & "C:\Program Files\MINTI\cland\minti-cland.exe" join --mnemonic ... --address 192.168.56.101:7777 --pin sha256:...
  - **M5-D live run**: with VMs up + Windows Service joined, from elevated PS:
      powershell -ExecutionPolicy Bypass -File scripts\m5-phaseD-validate.ps1
  - **Phase J residue**: the foreground minti-cland.exe from Phase J
    was killed during M5-B verification (PID 23752). The Phase J
    state at %LOCALAPPDATA%\MINTI\cland is still on disk (a separate
    member_id from the service's state at %PROGRAMDATA%\MINTI\cland).
    Do NOT restart that foreground binary while the Service is also
    running -- they will race for :7777.

Next milestones per super-plan: M5.1 (tray app + Fyne; live macOS
install when a Mac boots), M4.1 (remote-API backend → Cardputer-
as-Orchestrator demo) or M5 (cross-platform Clan Agent — Windows
Service / macOS launchd, smaller subset than full cland).
```

### Repo location

- Path: `C:\Users\aouad\Documents\CCode\MINT\MINT_wip`
- Branch: `main`
- Remote: **none** (local-only by user choice; add later if publishing)

### Test environments (both currently powered off)

| Env | How to start | Notes |
|---|---|---|
| **VirtualBox VM `minti-dev`** | `& "C:\Program Files\Oracle\VirtualBox\VBoxManage.exe" startvm minti-dev --type gui` (or `--type headless` for SSH-only) | Linux Mint 22.3 Xfce, 8 GB RAM, 4 vCPUs, 40 GB disk. Full MINTI stack installed. SSH to it via `ssh -i ~\.ssh\minti_vm -p 2222 minti@127.0.0.1` (passwordless). Passwordless `sudo` for `minti` user. Shared folder at `/media/sf_minti-repo` = the Windows-side repo (instant edit sync). NAT port-forwards: `127.0.0.1:2222→VM:22`, `127.0.0.1:17780→VM:7780`. |
| **WSL2 Debian 13** | `wsl -d Debian -u root` | Same MINTI stack installed. xrdp desktop attempted but flaky on WSL (known WSL artifact, not a MINTI bug — use the VM for desktop testing). |
| **Go toolchain (host)** | `$env:Path = "C:\Program Files\Go\bin;$env:Path"; go version` | Installed system-wide via winget. 1.26.3. |

### Local LLMs available for delegation (host Ollama on RTX 5090)

Start Ollama on host with `& "C:\Users\aouad\AppData\Local\Programs\Ollama\ollama.exe" serve` (background). Models:

- `deepseek-r1:32b` (18.5 GB) — reasoning, has `<think>` traces
- `gemma4:31b` (18.5 GB) — strong general, also has thinking
- `qwen3.6:latest` (22.3 GB) — strong code, thinking-heavy

**Gotcha:** all three emit thinking traces. For code-gen delegation, set `num_predict ≥ 8000` and/or strip thinking. See `memory/reference_local_llms.md`.

### Build the runtime + run install.sh end-to-end

```powershell
# native build
cd C:\Users\aouad\Documents\CCode\MINT\MINT_wip
$env:Path = "C:\Program Files\Go\bin;$env:Path"
cd runtime-adapter
go build -o minti-runtime.exe .\cmd\minti-runtime

# Linux cross-compile (for the VM)
$env:GOOS = "linux"; $env:GOARCH = "amd64"
go build -o dist\minti-runtime-linux-amd64 .\cmd\minti-runtime
$env:GOOS = ""; $env:GOARCH = ""

# Inside the VM (via SSH):
ssh -i ~\.ssh\minti_vm -p 2222 minti@127.0.0.1 "sudo bash /media/sf_minti-repo/install/install.sh"
ssh -i ~\.ssh\minti_vm -p 2222 minti@127.0.0.1 "curl -s http://127.0.0.1:7780/minti/health"
```

---

## Recent commits

```
dbd6e10  M5-C: minti-cland macOS launchd daemon (install / uninstall / build-tarball)
4e5373b  M5-B: minti-cland Windows Service via NSSM (install / uninstall / build-zip)
25e1cf1  M5-A: cross-platform foundations for cland (probes, paths, Makefile)
51a1ac0  M4-H-3 + security hardening: admitted→active promotion + gossip; replay+rate limits
8d103ca  M4 project-review pass: 3 local LLMs critique the whole project at v1 boundary
9b21a86  M4-J: 3-node testbed (2 VMs + Windows host) + 2 real protocol fixes — M4 DONE
912eafb  M4-I: install.sh + systemd unit + Makefile + minti-fetch Clan surface
a33634d  M4-H-2: revocation gossip via heartbeat digest
500e9b2  M4-H-1: key rotation 2PC (orchestrator-driven, /clan/rotate-key)
8dde3f4  M4-G: cross-Clan tool execution (/mcp/execute + signed permission tokens)
558ee1a  M4-F: routing layer (Orchestrator chat-completion proxy + self-route fast path)
9f07428  M4-E: leader-lease election engine + heartbeat + pin + 3 prereq fixes
```

---

## What works right now (verified)

- `install.sh` runs end-to-end on a fresh Debian-family host: detects distro/arch/GPU/network, installs base packages + Ollama, creates `minti` system user, installs `minti-runtime` binary, writes default `/etc/minti/runtime.yaml`, enables + starts the systemd unit. Idempotent.
- `minti-runtime` daemon on `127.0.0.1:7780`:
  - `GET /minti/health` → `{"status":"ok"}`
  - `GET /minti/version`, `GET /minti/capabilities` → static + live capability discovery
  - `POST /v1/chat/completions` (OpenAI shape, streaming + non-streaming) — translates to Ollama, proxies stream chunks back
  - `POST /api/chat` (Ollama-native passthrough)
  - `GET /v1/models`, `GET /api/tags` — model enumeration
- `minti-fetch` (bash) prints a colored ASCII logo + live system + runtime + Clan status (à la neofetch)
- Build is reproducible: `make runtime` (native), `make runtime-linux` (Linux amd64 cross-compile)

---

## Open questions / known issues

| | |
|---|---|
| MCP permission flow across Clan members | Spec'd in `docs/clan-protocol.md` §7; needs implementation in M2 |
| Reasoning-score rubric maintenance | YAML at `/etc/minti/reasoning-scores.yaml`, community-curated update channel in M6 |
| Tensor-parallel inference (model bigger than one node) | v2 — adopt exo or llama.cpp RPC |
| Cross-OS Clan Agent maintenance burden | Single Go binary, CI cross-builds — addressed in M5 |
| xrdp desktop in WSL2 | Known-flaky, not a MINTI issue. Use the VirtualBox VM for desktop dev. |

---

## Files to read for context

| File | What it tells you |
|---|---|
| `STATUS.md` (this file) | Where we are right now |
| `DOCUMENTATION.md` | What got built, in what order, why |
| `README.md` | Public-facing project description |
| `docs/clan-protocol.md` | Wire-level spec for the Clan protocol |
| `runtime-adapter/README.md` | How the runtime daemon works internally |
| `scripts/setup-vm.sh` | The one-shot installer that produced the current VM |
| `~/.claude/plans/hello-can-we-create-abundant-hopper.md` | **The PRD** — the authoritative source of truth |
| `~/.claude/projects/.../memory/*.md` | User profile, build workflow, dev env gotchas |
