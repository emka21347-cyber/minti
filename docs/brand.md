# MINTI Brand & Visual Identity

## Color palette

| Role | Name | Hex | ANSI 256 | Usage |
|------|------|-----|----------|-------|
| Primary accent | Mint green | `#00d787` | `\e[38;5;42m` | Logo, headings, active state, progress bars |
| Secondary accent | Bright cyan | `#5fd7ff` | `\e[38;5;81m` | Node indicators, agent status, links |
| Divider | Mid grey | `#8a8a8a` | `\e[38;5;245m` | Horizontal rules, separators |
| Label | Light grey | `#bcbcbc` | `\e[38;5;250m` | Field labels, secondary text |
| Value | Near-white | `#eeeeee` | `\e[38;5;255m` | Field values, body text |
| Background | Deep black | `#0a0a0a` | terminal default | All backgrounds |
| Surface | Dark surface | `#1a1a1a` | — | Subtle grid, card backgrounds |

Palette rule: mint green is reserved for **single-element focus** (one logo, one progress bar, one selected item). Never use it for body text. Cyan is for **enumerable things** — nodes, agents, peers. Greys carry everything else.

## Typography

| Context | Typeface | Notes |
|---------|----------|-------|
| Terminal / console | JetBrains Mono, Fira Code, or system monospace | Any ligature-capable mono works |
| GRUB boot menu | DejaVu Sans Bold 18pt (`.pf2`) | Generated via `grub-mkfont` |
| SVG wordmark | Rect-grid pixel letters (no font dependency) | Generated by `scripts/make-brand-art.py` into `assets/logo.svg` |

## Logo

The MINTI mark is the **▚ quadrant** (U+259A): two solid squares meeting at
exactly one corner — the upper-left and lower-right of a 2×2 grid. It is the
workspace top-bar glyph, the site favicon, and the boot mark. Two machines
touching = the smallest possible Clan.

> Replaced the 9-node mesh lockup on 2026-06-11 (operator decision). Mesh
> imagery survives only as illustration (e.g. the site's how-it-works
> diagrams), never as the brand mark.

```
██████
██████
██████
      ██████
      ██████
      ██████

 M I N T I
 ─────────
 Open agents.
 Open weights.
 Clan-aware.
```

- Quadrant squares in bright cyan `#5fd7ff`
- Wordmark `M I N T I` in mint green `#00d787` bold — in SVG/PNG it is built
  from rects on a 5-row pixel grid (no font dependency)
- Rule `─────────` in mid grey `#8a8a8a`
- Tagline in dim/grey
- Geometry: squares of side *s* touch at one point; in terminal renders the
  lower square indents by the square's own width (6 cells)

## Source files

| File | Description |
|------|-------------|
| `scripts/make-brand-art.py` | **Single source of truth** — generates both SVGs below |
| `assets/logo.svg` | Vector lockup (quadrant + pixel wordmark), renders 16px–2560px |
| `assets/wallpaper-3840x2160.svg` | Master wallpaper (SVG source) |
| `assets/plymouth/minti/` | Plymouth boot splash theme — image-free (pure Rectangle primitives) |
| `assets/grub-theme/minti/` | GRUB2 boot menu theme |
| `branding/minti-fetch` | neofetch-style console info script |
| `site/assets/favicon.svg` + `favicon-32.png` + `apple-touch-icon.png` | Site tab icons (▚), generated by `scripts/make-favicons.py` |

Wallpaper PNGs (2560×1440, 1920×1080) and the GRUB `background.png` are **build
artifacts**: `scripts/build-iso.sh` rasterizes them from the wallpaper SVG on
the build VM (resvg/inkscape). They are not checked in. The build script's
Plymouth raster step (`minti-wordmark.png`, `node.png`) is obsolete since the
image-free splash — remove it at D4.

## Usage rules

1. **Don't recolor the logo.** Mint green + cyan on black only. No white versions, no inverted versions.
2. **Minimum clear space.** At least one logo-width of clear space on all sides.
3. **Tagline is optional** below the wordmark. Never abbreviate or reorder the three lines.
4. **No gradients.** Flat color only — the mesh evokes structure, not decoration.
5. **ASCII fallback.** When ANSI is unavailable, the plain-text logo must still be readable.
